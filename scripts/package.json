{
  "//": "The root package.json sets \"type\": \"module\" for the Vite app. The scripts in this folder are CommonJS (require/module.exports). This file scopes them back to CommonJS so `node scripts/extract-data.js` keeps working unchanged.",
  "type": "commonjs"
}
