# Status

Last updated: 2026-08-21 (class-feature choices: Divine Order, Primal Order and Elemental Fury have a picker, storage, a class-step gate and sheet display — what they GRANT is still unapplied)

## Build order

1. [done] Markup renderer
2. [done] App skeleton + persistence
3. [done] Character creation — 5 pickers + wizard shell + class skill/mastery/fighting style/subclass/optional-feature/expertise pickers wired in and all persisted to storage.
4. [done] Calculation layer — ability scores/modifiers, proficiency bonus, saving throws, initiative, skills, passive values, speed/size/darkvision, hit dice pool.
4a. [done] Feat/ASI slice — selection, storage, AND the effects computable from what's stored now feed the calculation layer (ability scores, saving throws, skills, initiative note). See "Feat/ASI effects" below for what's still deliberately out — attacks/AC-targeting feats (step 7/8) and the 5 skill-choice feats the wizard has nowhere to store a pick for (docs/REPORT.md).
5. [done] Sheet display — first half (skeleton, header, ability scores/modifiers, proficiency bonus, saving throws, initiative) plus second half (skills, passive values, speed/size/darkvision, hit dice pool, feat list).
6. [done] Spells — spell attack bonus/DC, spell slots (incl. third-caster and Pact Magic), class spell list access and level-legal filtering, class spell picker (counts enforced), subclass always-prepared spells (all fixed-grant shapes: `prepared`/`known`/`innate`/`expanded`-rank, incl. resource/daily/ritual-wrapped ones), subclass spell-choice pickers (College of Lore, the four Wizard subclasses), feat-granted spells (fixed and player-choice, incl. the 12 Eberron marks and Magic Initiate), and full sheet display of all of the above grouped by level with provenance. Standing deferrals, none blocking: Warlock The Genie's 4 per-genie-kind grants (no stored genie-kind choice), Boon of Siberys (hidden, needs a 13-way picker), the 12 Eberron marks' campaign-setting gating (unreachable except via import), two spells referenced but absent from the data, and the previously-recorded missing subclass feature descriptions (Sorcerer Divine Soul etc.). See docs/QUESTIONS.md for each. Slot spending is step 9, not this step.
6a. [done] Class feature choices — selectable class/subclass features (Warlock Invocations, Fighter Maneuvers, and similar "choose from a list" features). Coverage inventoried (scripts/investigate-class-feature-choices.js); slice 1 (the DATA LAYER — class-level progression counts + prerequisite evaluation) and slice 2 (picker, storage, wizard step, sheet section) are done. All 3 `choose`-form spell prerequisites (Agonizing Blast, Eldritch Spear, Repelling Blast) are now answered structurally — Repelling Blast's `spell attack=m;r;o` clause reads a second context field, `damagingAttackCantripsByClass` (optionalFeatureData.ts), fed by spellDetailData.ts's `damagingAttackCantripsAmong`. The picker now runs as its OWN wizard step after 'spells' (D64), so no invocation needs back-navigation to become eligible. The `additionalSpells` grants are fully applied: 16 of the 17 options grant a fixed spell that now reaches the sheet with the option named, and Pact of the Tome's `choose` grant has its own picker (3 cantrips + 2 level-1 rituals, stored, gated and displayed). The `senses` field (3 optional-feature options, 3 feats) is now also applied, closing 6a — see "Senses granted by a chosen optional feature and by a chosen feat" below.
7. [not started] Inventory and equipment
8. [not started] Level up
9. [not started] Play tracking and rests
10. [not started] Multiclass

## What exists

- Data extraction — scripts/extract-data.js + scripts/validate-data.js, 119 checks green (phase 0, complete). Feature collection now also walks every kept class/subclass feature's text for ref* nodes (refClassFeature/refSubclassFeature) and pulls in the target, repeating until the set stops growing — class-features.json 279->302, subclass-features.json 465->786 (see docs/DATA.md, "Traps"). check-dangling-refs.js's new check verifies every ref* node (all four types) inside any feature's text resolves, including a D12 fallback so Fighting Style optionalfeature refs resolve against feats.json category "FS".
- Investigation script — scripts/investigate-slice1.js, confirms weapon mastery and fighting style data shape ahead of the remaining step-3 walkthrough work.
- Investigation script — scripts/investigate-class-skills.js, confirms all 13 classes' skill-choice shape in classes.json.
- Investigation script — scripts/investigate-subclass-shape.js, confirms subclass linkage, D28 filtering counts, and that every class grants a subclass at level 3.
- Investigation script — scripts/investigate-expertise.js, confirms the shape of expertise features and species skillProficiencies ahead of the expertise picker.
- Investigation script — scripts/investigate-expertise-2.js, confirms two more expertise-granting features not named "Expertise" (Ranger's Deft Explorer, level 2 not 1; Wizard's Scholar, restricted to 6 skills) and that "Infiltration Expertise" (Rogue/Assassin 9) grants no skill expertise at all despite its name.
- Investigation script — scripts/investigate-species-skills.js, confirms parent/variant inheritance (variant entries already carry their own, identical, skillProficiencies — nothing to merge), all element counts are 1, and five distinct entry shapes across the 26 species that carry the field (fixed single skill, fixed multi-skill, choice with count, choice without count, and `{ any: N }`).
- Investigation script — scripts/investigate-spells.js, confirms ahead of build order step 6 (Spells) what classes.json/spells.json actually hold: per-class casterProgression/spellcastingAbility, that cantrip/known/prepared counts are always structured numeric arrays (never prose-only), the classTableGroups slot-table shape, that Warlock's Pact Magic uses a flat single-slot-count/slot-level table (not the rowsSpellProgression matrix full casters use), subclass `additionalSpells` shape (72 of 114 subclasses, 14 distinct outer-key shapes), and the real classes/classVariants trap counts in this repo's filtered data/spells.json (91, not the 109 DATA.md records — see docs/REPORT.md).
- Investigation script — scripts/investigate-subclass-spell-shapes.js, classifies the 40 (of 72) additionalSpells-carrying subclasses slice (d2b) left unhandled: 35 are a fixed grant under a different key (`known`/`innate`/`expanded` with literal spell names, or `prepared` mixed with those — needs only a derived lookup like d2b) and 5 need a real picker (`choose` in the data — Bard College of Lore, Wizard Abjurer/Diviner/Evoker/Illusionist). See docs/REPORT.md for the full breakdown.
- Investigation script — scripts/investigate-tool-proficiencies.js, confirms all 33 backgrounds carry toolProficiencies (always a 1-element array), three category keys occur (anyArtisansTool, anyMusicalInstrument, anyGamingSet, all count 1), and that items.json can filter all three structurally by item `type` code — anyMusicalInstrument's "INS" code also covers 15 magic instruments, so that filter additionally needs `rarity === "none"`.
- App skeleton — Vite + React + TypeScript, data served from public/data/.
- Markup renderer — src/markup/, 107 tests. Now also renders `abilityDc` (a save DC named for an ability score, e.g. Arcane Shot) as a sentence naming the ability, and `statblock` (a pointer to a creature/object statblock in another file) the same way as the ref* types — target name shown, tag/name/source kept in `data-ref-*` attributes since D7 still has no cross-file lookup.
- Feature-reference resolver — src/featureResolver/, a layer outside src/markup/ (D7 is unchanged: the renderer still shows only a ref*'s name with `data-ref-uid` on the element). `scanRefs`/`distinctRefs` find every refClassFeature/refSubclassFeature/refOptionalfeature/refFeat node in an entries tree at any depth (131 of 420 sit below the top level — list items, `options` blocks — not just directly in the array). `resolveRef` is a pure lookup against classFeatures/subclassFeatures (by the D33 `id` scheme) and optionalFeatures/feats (by name+source, with the D12 fallback to feats.json category "FS" for Fighting Style refs). `buildExpansions` resolves and recurses into the target's own text for further ref*s, one expansion per feature per branch (a `seen` set per recursion path prevents loops on a cycle). `<ResolvedEntries>` renders the untouched `<Entries>` output plus a collapsed `<details>` per resolved ref (a ref that doesn't resolve gets a visible "text not found" note instead, per D43 — never blank, never a crash). `loadResolverData` fetches its four files through the shared loader (src/dataLoader/dataLoader.ts, D39). Wired into the three class-step components that render feature-shaped `Entries` content: SubclassPicker, FightingStylePicker, OptionalFeaturePicker.
- Storage layer — src/storage/, localStorage key `familliar:characters`, schema version 9. A saved character carries classSkills, speciesSkills, expertiseSkills, masteries, fightingStyle, (via CharacterClass.subclass) subclass, the background's two fixed skill proficiencies plus its tool proficiency (CharacterBackground.skillProficiencies / .toolProficiency), and optionalFeatureChoices — an array of `{ featureType, choices }`, so picks from more than one subclass progression could coexist without ambiguity. A version-6 save is rejected, not migrated (see docs/QUESTIONS.md, "Migrace uložených postav").
- Character creation wizard — src/creation/, steps: class → species → background → [expertise] → languages → ability scores → [spells] → [class optional features] → [featAsi] → review. A bracketed step only appears when the character is entitled to it and is skipped otherwise, with the remaining steps renumbering to stay contiguous (`visibleSteps` in wizardState.ts); expertise appears when the class grants Expertise by the chosen level. The class step also holds class skill proficiencies, weapon masteries, fighting style, subclass and the SUBCLASS's optional-feature picks (D13) — all clear when the class or level changes, and all are now saved by the review step. The CLASS's own optional features moved off it to their own step (D64). The species step also holds the species' own skill proficiencies (fixed and/or chosen); they clear when the species changes. The expertise step's own selection clears whenever class, level, class skills, species, species skills, or background change (anything that affects the offered proficient-skill pool or the required count). The wizard's `subclass` field carries the chosen subclass's source and featureType alongside its name (SubclassChoice in wizardState.ts), computed once when the subclass is picked, so nothing needs to re-derive the subclass's book by loading the subclass list a second time.
- Five creation pickers — src/classes/, src/abilities/, src/species/, src/backgrounds/, src/languages/.
- Class skill picker, species skill picker, mastery picker, fighting style picker, subclass picker — src/classSkills/, src/speciesSkills/, src/masteries/, src/fightingStyle/, src/subclass/, wired into the class/species steps. Class skill picker receives the background's two fixed skills as disabledSkills (D18) once a background is chosen. Species skill picker receives the class skills and (if already chosen via back-navigation) the background's fixed skills as disabledSkills. The background step now also annotates its own two fixed skills when they collide with class or species skills already chosen (D44) — species is picked before background in the wizard order, so this is the only place that collision can surface. subclassData.ts's SubclassOption now also carries `featureType` (the subclass's own optionalfeatureProgression code, or null), read off the same classes.json fetch already made for the subclass list. The class/feature join in subclassData.ts now tries strict classSource matching first and falls back to className+subclassShortName+subclassSource (ignoring classSource) when that finds nothing — fixes 26 of 94 offered subclasses (e.g. Arcane Archer, Cavalier, Samurai, Rune Knight) that were rendering with no feature text because their text is filed under an older classSource than the XPHB-converted subclass entry (D27). All 94 offered subclasses now resolve at least one feature.
- Optional-feature picker — src/optionalFeatures/, one generic picker for every subclass's `optionalfeatureProgression` (Battle Master's Maneuvers, Rune Knight's Runes, Arcane Archer's Arcane Shot, College of Swords' Fighting Style — the four found by the earlier investigation). `FS:*` codes resolve against feats.json category "FS" (D12); every other code resolves against optional-features.json. Wired into the class step, shown once a subclass is chosen; clears when class, level or subclass changes; now persisted to storage.
- Tool proficiency picker — src/toolProficiencies/, wired into the background step. A named tool (e.g. "Disguise Kit") auto-fills with nothing to pick, matching BackgroundGrants' own display; a category choice (anyArtisansTool, anyMusicalInstrument, anyGamingSet) lets the player pick exactly 1 from a list filtered structurally out of items.json. The background step cannot advance until this choice is made. Persisted as `CharacterBackground.toolProficiency`.
- Expertise picker — src/expertise/, its own wizard step between background and languages, shown only when the class grants Expertise by the chosen level. The per-feature skill count isn't structured in class-features.json, so it's hardcoded in expertiseData.ts from PHB 2024 prose: features named "Expertise" (Bard 2/9, Ranger 9, Rogue 1/6) grant 2, unrestricted; "Deft Explorer" (Ranger 2) grants 1, unrestricted; "Scholar" (Wizard 2) grants 1, restricted to 6 named skills. "Infiltration Expertise" (Rogue/Assassin 9) is excluded — despite its name it grants no skill expertise (Masterful Mimicry, Roving Aim). The picker only ever offers skills the character is already proficient in, unioned from class skills, the background's fixed pair, and species skills; if that pool is smaller than the count calls for, it offers everything available and says so rather than blocking.
- Component tests — jsdom + Testing Library, e.g. src/creation/CharacterWizard.test.tsx.
- Shared data loader — src/dataLoader/dataLoader.ts (D39). `loadDataFile(path)` fetches a data/ file at most once and caches the in-flight/resolved promise per path, so a second request for the same path — or a concurrent one — never triggers a second download. A failed fetch is not cached, so the next request retries; the rejection itself still propagates to whichever caller triggered it, unchanged from the old per-module fetch. Every `*Data.ts` reader that used to fetch independently (backgrounds, classes, classSkills, expertise, featureResolver, fightingStyle, languages, masteries, optionalFeatures, species, speciesSkills, subclass, toolProficiencies — 13 modules) now goes through it; behaviour is unchanged, only the data source. src/MarkupDemo.tsx (temporary scaffolding, see below) still fetches directly. Calculation functions still take parsed data as a parameter (D38) — the loader is for callers that acquire data, not for the functions that compute from it.

- Calculation layer — src/calculation/ (D47), now complete for build order step 4. Pure functions (D38): take a `Character` and, where the data comes from data/, a small typed slice of it as a parameter — none of them fetch. Every result is `Calculated<T>` (src/calculation/types.ts): `{ status: 'known', value, breakdown }` with a `Contribution[]` breakdown (D40), or `{ status: 'unknown', reason }` when the input is missing or a class isn't found in the supplied data (D43) — never a throw. `abilityScores.ts` computes the D17 sum (raw score + background bonus) as a contribution list (D42; a zero/absent background bonus is left out of the list, not written as a zero entry) plus the modifier. `proficiencyBonus.ts` sums total level across `character.classes` (D11) and maps it to +2..+6, with one breakdown contribution per class. `savingThrows.ts` computes all 6 saves — ability modifier, plus proficiency bonus when a class in `character.classes` grants that save (matched against a caller-supplied `ClassSavingThrowProficiencies[]`, shaped after classes.json's `proficiency` field, confirmed by scripts/investigate-saving-throws.js: a 2-element array of lowercase ability codes, same shape on all 13 base classes). `initiative.ts` is the DEX modifier. Feats/ASI (Resilient, Alert) are out of scope until 4a. AC, max HP, attacks, spell DC stay out of this folder until their own steps (D47).
- Calculation layer, second half — closes build order step 4. `skills.ts` (D45, D48) computes all 18 skills, reusing `ALL_SKILLS` from classSkillData.ts rather than a second copy of the list. Each result carries a status (`none`/`half`/`proficient`/`expertise`) plus the numeric modifier; proficiency sources are `character.classSkills`, `character.background.skillProficiencies`, `character.speciesSkills` — when more than one names the same skill (D44), the bonus is added once and the breakdown joins every source into one contribution (`proficiency (class, background)`), mirroring how savingThrows.ts already joins multiple granting classes. Expertise (from `character.expertiseSkills`) doubles the proficiency bonus. Jack of All Trades (half proficiency bonus, rounded down, only where the character has no other proficiency source, never combined with expertise per D45) is hardcoded as a Bard/XPHB level-2 grant — scripts/investigate-calc-slice2.js confirmed class-features.json has exactly one "Jack of All Trades" entry, so this follows the same hardcode-from-confirmed-data precedent as expertiseData.ts rather than adding a data param. Passive Perception/Investigation/Insight (D48) live in the same file, each 10 + that skill's own breakdown. `speciesTraits.ts` computes speed, size and darkvision from a caller-supplied species data slice. Two data traps surfaced by scripts/investigate-calc-slice2.js and resolved with the user before writing the function: (1) the four Genasi subraces (Air/Earth/Fire/Water, MPMM) omit size and darkvision entirely, and Earth/Fire omit speed too — those fields exist only on the parent "Genasi" entry, linked via the same raceName/raceSource speciesData.ts already uses for display names; resolved to fall back to the parent's value for whichever field the subrace entry itself lacks. (2) 23 species (including XPHB Human, Aasimar, Tiefling) list size as a 2-element array (a real Small-or-Medium player choice with no wizard step yet); resolved to report `unknown` rather than default to one. A missing darkvision field (28 of 78 species) is a definite zero, not unknown — most species simply have none. `hitDice.ts` computes the hit dice pool (D11): one `{ className, faces, count }` entry per class in `character.classes`, faces from a caller-supplied `ClassHitDie[]` (classes.json's `hd.faces`), count from the character's own level in that class.

- Investigation script — scripts/investigate-feat-structured-fields.js, confirms which of the 128 feats in feats.json carry their effect in a structured field vs. prose only, ahead of the feat/ASI slice (4a).
- Investigation script — scripts/investigate-feat-asi-eligibility.js, confirms which class-features.json levels grant an ASI-or-feat choice per class (not the flat 4/8/12/16/19 the task brief assumed — see below), the startingProficiencies/spellcastingAbility shapes classes.json carries, and the species.json fields (`creatureTypeTags`, `size`) the `race` prerequisite needs.
- Feat/ASI slice (4a, D16/D19/D20) — src/featAsi/. Selection and storage only; no feat or ASI effect is applied to any calculation yet (that's the next task). `featAsiData.ts` is pure logic: `featAsiGrantsFor` reads class-features.json for every level a class grants an ASI-or-feat choice — confirmed per-class, not the flat 4/8/12/16/19 the task brief assumed: most classes grant "Ability Score Improvement" at 4/8/12/16, Fighter also at 6/14, Rogue also at 10, and level 19 is a separate feature named "Epic Boon" (not "Ability Score Improvement") that grants the identical kind of choice — confirmed against PHB 2024 with the user, who also confirmed the EB category tag on Epic Boon feats is NOT a filter (D19 still applies unfiltered; EB feats are only sorted first in the level-19 list as the feature's suggested pool). `evaluateFeatPrerequisites` evaluates all 13 prerequisite shapes feats.json carries: the array is alternatives (OR), each entry's keys are AND'd, and only `race` ever holds more than one alternative within one entry. Ability scores fed in are the calculation layer's FINAL values (D16/D17) — never combined with an ASI picked earlier in the same wizard session, since the calculation layer doesn't carry that yet (documented limitation, not fixed here). `race` prerequisites resolve against species.json through two indirections: the "Elf; Drow Lineage" naming (family before "; ", subrace key after) and `creatureTypeTags` (links a renamed variant like Deep Gnome/Sea Elf back to "gnome"/"elf"). "half-elf" and "half-orc" never resolve (no such species.json entries in this app's pool) so those alternatives are always unmet, shown with a reason, never hidden (D19). `campaign` and `otherSummary` prerequisites (Eberron Dragonmarks; two Fighting-Style-timing feats already handled by the class step's own FightingStylePicker) are always unmet, each with its own visible reason. `FeatAsiPicker.tsx` shows one panel per eligible level: ASI-vs-feat first (D20), then that choice's own options. The ASI sub-picker enforces the level-20 cap by disabling any ability option that would exceed it, checked against the running total of ASI picks made earlier in the same panel. The feat sub-picker lists every feat unfiltered by category (D19); an ineligible one stays visible and disabled with its unmet-prerequisite reasons shown, never hidden. Wired into the wizard as a new `featAsi` step, positioned AFTER `abilities` (not next to `expertise`) because feat prerequisites need the final ability scores the abilities step produces; shown only when the class has at least one grant by the chosen level, numbering stays contiguous (same mechanism as the expertise step). State (`WizardData.featAsiChoices`) clears when class or level changes. Storage: `Character.featAsiChoices`, one entry per eligible level, each either `{ kind: 'asi', increases }` or `{ kind: 'feat', name, source }`.

- Feat/ASI half-feat ability choice — of the 82 feats with a feats.json `ability` field, 13 give a fixed bonus and 68 let the player choose which of 2-6 named abilities gets +1 (confirmed via scripts/investigate-feat-ability-choice-shape.js; no feat mixes a fixed bonus with a choice, none carry more than one `ability` array entry outside Ability Score Improvement itself, which the picker already excludes). `featAsiData.ts` adds `featAbilityChoiceOptions(feat)` (the named abilities, or null for a fixed/no-bonus feat) and `featsRequiringAbilityChoice(feats)` (a `${name}|${source}` key set). `FeatAsiPicker.tsx`'s feat sub-picker shows an "Ability" select once such a feat is chosen; picking a different feat replaces the whole choice object, which clears any previously chosen ability for free. `FeatAsiChoice`'s `kind: 'feat'` branch gained an optional `chosenAbility: Ability` (storage/character.ts); `wizardState.ts`'s completion check (`isStepComplete`/`isReadyToSave`/`saveCharacter`) takes a `featsRequiringAbilityChoice` set (default empty, threaded from CharacterWizard.tsx via a second `loadFeats()` fetch, same pattern as the other step-visibility lookups) and blocks the 'featAsi' step until every such feat's `chosenAbility` is set. Schema version bumped to 9 (from 8) — a version-8 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav").

- Feat/ASI effects into the calculation layer — closes 4a. `src/calculation/featEffects.ts` (D38: pure, feat data passed in as `FeatEffectEntry[]`, nothing fetches — no Sheet exists yet to be the caller, so tests build fixtures directly). Scope confirmed against the real data via scripts/investigate-feat-calc-fields.js, not assumed:
  - Ability score bonus (82 feats: 13 fixed, 68 choice) — `featAbilityScoreContributions` adds one D42 list entry per ASI pick (`ASI (level N)`) and per feat (`feat (Name)`), fixed feats read their amount from feat data, choice feats read `chosenAbility` (D57) and are always +1. `computeAbilityScore`/`computeAbilityScores` (abilityScores.ts) gained an optional `feats` parameter (default `[]`, so CharacterWizard.tsx's own draft-scores call is unchanged — it deliberately excludes same-session feat/ASI picks from that session's own prerequisite checks, per the existing documented limitation).
  - Saving throw proficiency — only Resilient carries this field, and its `savingThrowProficiencies.choose` list is identical to its own `ability.choose` list (confirmed by reading the feat's prose, not guessed): the already-stored `chosenAbility` resolves both, so no new stored field was needed. `computeSavingThrow`/`computeSavingThrows` (savingThrows.ts) gained an optional `feats` parameter; a feat and a class granting the same save collide per D44 (counted once, both sources named, e.g. `proficiency (Fighter, feat (Resilient))`).
  - Skill proficiency/expertise — of 6 feats with `skillProficiencies` and 3 with `expertise`, only ONE grant is actually computable: Boon of Skill's `skillProficiencies` is a FIXED list (all 18 skills). Every other entry (Keen Mind, Observant, Prodigy, Squat Nimbleness, Skill Expert's `skillProficiencies`; all 3 feats' `expertise`, always `anyProficientSkill`) is a player choice this app has nowhere to store — confirmed with the user this task does not extend the wizard to add one (wizard and schema both left alone). Those get a distinct "awaiting a choice" D40 note (not D55's "known effect, not computed" wording — the mechanism here IS structured and known, only the pick is missing), attached to every skill the choice could land on: named skills for a `choose` list, every skill for an `any`/`{any:N}` list, and — for `expertise`'s `anyProficientSkill` — every skill the character is ALREADY proficient in (the one thing that candidate set actually is, computed inline rather than guessed). `computeSkill`/`computeSkills`/the passive-value functions (skills.ts) gained an optional `feats` parameter; passive values inherit the notes for free since they splice in the skill's own breakdown.
  - Prose feats (15, no structured field at all) — `PROSE_FEAT_EFFECT_TARGETS`, a hand-maintained table (comment explains why it's manual and that it must be re-checked against all 15 feats' actual text whenever a new value gets computed), currently `{ Alert: ['initiative'] }` — the only one of the 15 that touches a value this app computes today (checked all 15 against initiative/saves/skills/passive values/speed/size/darkvision/hit dice; attacks/AC-targeting ones are listed in docs/REPORT.md for step 7/8 instead). `proseFeatEffectNotes` adds a D55 note (`amount: 0`, distinct wording from the "awaiting a choice" note above) wired into `computeInitiative` (initiative.ts, gained an optional `feats` parameter).
  - Confirmed NOT applicable to anything computed today, so left alone: no feat has a `speed` field at all (checked); `senses` (3 feats) only ever grants blindsight/truesight, never darkvision, and speciesTraits.ts computes darkvision only — nowhere for these to plug in yet.
  - `Contribution` (calculation/types.ts) gained an optional `note` field — set only on the two "not a number" cases above, `amount` stays 0 so it never changes a total.

- Character sheet, first half (build order step 5a) — `src/sheet/`. `CharacterSheet.tsx` renders the skeleton, header (name, classes iterated per D11, species, background) and the top value block: all 6 ability scores/modifiers, proficiency bonus, all 6 saving throws (marked ● where the character has proficiency, from any source), and initiative. Every number is read from `src/calculation/` (D38) unmodified — nothing recomputed in the component. `sheetData.ts` shapes the two data/ files the top block needs beyond what's stored on the Character: classes.json's `proficiency` field into `savingThrows.ts`'s `ClassSavingThrowProficiencies[]`, and feats.json into `featEffects.ts`'s `FeatEffectEntry[]` (the fields it reads — ability, savingThrowProficiencies, skillProficiencies, expertise — are already top-level feats.json keys in that exact shape, confirmed by scripts/investigate-feat-calc-fields.js, so feat effects from 4a feed straight into the sheet's numbers). Both go through the shared loader (D39). `ValueBreakdown.tsx` is the one shared `<details>` component D41 asked for — `<ValueBreakdown breakdown={...} />` for a Contribution list (default collapsed, browser owns the toggle) and `<UnresolvedValue reason={...} />` for D43's visible-not-blank "unknown" state; 5b's skills reuse both rather than a second implementation. Wired into `CharacterManager.tsx` as a new "Sheet" button per row, alongside the existing "Inspect" button at the time.

- Character sheet, second half (build order step 5b) — closes build order step 5. `CharacterSheet.tsx` gains: all 18 skills (D45 status mark — ○/◐/●/★ for none/half/proficient/expertise — plus the numeric modifier and a `ValueBreakdown` that names every proficiency source, D44); the three passive values (D48); speed/size/darkvision (a 2-element species `size` reports as unresolved per D54, never defaulted to Medium); the hit dice pool, one line per class (D11); and the character's chosen feats, one collapsed `<details>` per feat (name + level), its text rendered through `ResolvedEntries` (src/featureResolver/, D51) so any ref* the feat's own text points at expands too — collapsed by default so the sheet doesn't drown in text. `sheetData.ts` gained three more loaders, all through the shared cache (D39, so the repeat `feats.json` fetch costs nothing extra): `loadHitDiceClassData` (classes.json's `hd.faces`, into `hitDice.ts`'s `ClassHitDie[]`), `loadSpeciesTraitsData` (species.json into `speciesTraits.ts`'s `SpeciesTraitsData[]`), `loadFeatTextEntries` (feats.json's `entries` field, for the feat-list text — a different slice of the same file `loadFeatEffectEntries` already reads). D55 vs D58's two distinct "not computed" notes needed no new sheet code — `computeSkill`/`computeInitiative` already thread the right wording through their breakdown (`featEffects.ts`, 4a's closing task), so `ValueBreakdown` shows each verbatim, never merged.

- Saving throw proficiency status — `computeSavingThrow`/`computeSavingThrows` (savingThrows.ts) now return a `SavingThrowValue { status: 'none' | 'proficient', modifier }`, the same pattern D45 already gave skills, instead of `Calculated<number>`. The sheet used to infer proficiency by checking whether a save's breakdown had more than one entry (true whenever a class or Resilient added a `proficiency (...)` contribution) — that broke the moment a breakdown could grow a zero-amount note (D55/D58 style) for a reason unrelated to proficiency. `CharacterSheet.tsx` now reads `result.value.status` directly (mirroring the skills section exactly) and no longer inspects breakdown length at all. The breakdown's own contents, and what feeds them (class/feat data), are unchanged.

  Temporary-scaffolding cleanup attempted but incomplete: `CharacterInspector.tsx` (D14) is unwired — the import, "Inspect" button, and `inspectedId` state are removed from `CharacterManager.tsx` — but the file itself is still on disk. A settings deny rule blocked the delete command from this session; see docs/REPORT.md for what's left to do by hand.

- Spell attack bonus / spell save DC (build order step 6, slice a — closes D47's deferral of spell DC out of step 4) — `src/calculation/spellcasting.ts`. Pure (D38): takes a `Character` and a caller-supplied `ClassSpellcastingAbility[]` (classes.json's `spellcastingAbility` field, confirmed by scripts/investigate-spells.js — int/wis/cha on the casting classes, absent on Barbarian/Fighter/Monk/Rogue). `computeSpellcasting` returns `Calculated<SpellcastingEntry[]>` — one entry per casting class in `character.classes` (D11: a Wizard/Cleric multiclass gets two entries, two different abilities and numbers), reusing `computeAbilityScore` (including its `feats` parameter, so an ability-boosting feat already flows through, same as savingThrows.ts) and `computeProficiencyBonus` — neither recomputed. A class with no spellcasting ability contributes no entry (not a zero, not an error); a class named on the character but absent from the supplied `classData` entirely returns `unknown` (D43), matching hitDice.ts's pattern. Each entry carries both a `spellAttackBonus` and a `spellSaveDC`, each with its own D40 breakdown (ability modifier + proficiency bonus; DC also has the +8 base).

- Spell slots (build order step 6, slice b) — `src/calculation/spellSlots.ts`. Pure (D38): takes a `Character` and a caller-supplied `ClassSpellSlotsData[]` (a typed slice of classes.json's `classTableGroups` slot tables + `casterProgression`, confirmed by scripts/investigate-spell-slots.js). `computeSpellSlots` returns `Calculated<SpellSlotsEntry[]>`, one entry per casting class in `character.classes` (D11) — no combining across classes, since the shared multiclass slot table is step 10 (a comment in the loop cites D11/step 10, not thrown on). Ordinary slots (full/half casters, `casterProgression` "full"/"artificer") and Pact Magic slots (Warlock, "pact") are two distinct optional fields on the same entry (`ordinarySlots`/`pactSlots`), never merged into one shape — a future multiclass Warlock+other class could carry both at once even though phase-1 UI is single-class. `ordinarySlots` is always a zero-padded 1-9 array; the source table's own row width is NOT assumed to be 9 — scripts/investigate-spell-slots.js found Paladin's table only has 5 columns (it never grants spells above level 5), so the row is read at its real width and padded, not read as if it already had 9 entries. `casterProgression` only selects which of the two table shapes to read (DATA.md, "Traps" — it names a shared progression table, not a lookup key); the actual counts always come from the class's own table row for the character's level. A non-caster class (`casterProgression` null) contributes no entry, matching spellcasting.ts's pattern for a class with no spellcasting ability; a class named on the character but absent from the supplied data returns `unknown` (D43).

- Spell slots, third-caster follow-up (D46) — `spellSlots.ts` now also reads a subclass's own slot table (Eldritch Knight/Arcane Trickster), using the shape scripts/investigate-third-caster-slots.js confirmed last session: `ClassSpellSlotsData` gained an optional `subclasses: SubclassSpellSlotsData[]` (subclass name + its own `casterProgression`/`spellSlotsByLevel`), and `CasterProgression` gained `"1/3"`. Only consulted when the base class itself has no `spellSlotsByLevel` — a base class that already has one (every full/half caster) is untouched, and a Fighter/Rogue subclass with no spellcasting (e.g. Champion) still yields no entry, not an error. A "1/3" caster's slots come out as ordinary slots (same zero-padded 1-9 shape as full/half casters), never pact — reuses the existing row-reading/padding code rather than a second copy. `character.subclass` (already stored, CharacterClass.subclass) is matched by name against the supplied data; the function still only takes data in, it does not fetch or re-derive the subclass. No loader wires this data in yet and no picker/sheet reads it — that's still ahead, same as the rest of slice (b)/(d1).

- Class spell list access layer (build order step 6, slice c) — `src/spells/classSpellListData.ts`. Not calculation (D47's `Calculated<T>` shape doesn't apply — this is a data query, so it lives outside `src/calculation/`), following the same pure-filter/thin-loader split as the other `*Data.ts` readers. `extractClassSpellList` (pure, D38) takes the parsed `spells.json` array plus one class identity (name + `classSource`) and returns every spell on that class's list — cantrips and leveled spells in ONE array, each item carrying its own `level` (D61). Confirmed via a short shape check (D46, scripts/investigate-spell-class-shape.js) that spells carry the class link under `availableTo.classes`/`availableTo.classVariants` (each entry `{ name, classSource }`, `classVariants` additionally carrying `definedInSources`) — not the bare `classes`/`classVariant` field names DATA.md's trap note uses, though the trap itself (91 spells reachable only via `classVariants`) is exactly as DATA.md describes. A spell counts as on the list if the class matches EITHER array; each returned item carries `viaVariant` so the UI can flag variant-sourced spells differently later (D62's note). `ritual` reads `meta.ritual === true`; `concentration` reads whether any element of the `duration` array carries `concentration: true`. Neither is computed — both are carried through as-is. Takes one class per call (D11) — a multiclass caller unions results itself; the function doesn't. `loadClassSpellList` fetches through the shared loader (D39). No wizard step, no preparation, no sheet wiring yet — that's the next slice (d), which sits on top of this.

- Spell level-legal filter (build order step 6, slice d1) — `src/spells/spellLevelFilter.ts`. Pure function (D38), no fetching: `filterSpellsByLevel(classSpellList, spellSlots)` takes one class's slice-(c) spell list and that same class's slice-(b) `SpellSlotsEntry | undefined`, returns the sublist the character is legally allowed to take by spell level. Cantrips (level 0) always pass through untouched, confirmed with the user; leveled spells (1-9) are kept only if `level <= highest available slot level` (ordinary slots: highest index with count > 0; Pact Magic: the entry's own `slotLevel`). Investigated first (scripts/investigate-spell-cap-by-slots.js, D46): "highest slot level = highest castable spell level" holds for full casters, half casters (Paladin/Ranger/Artificer), and Warlock Pact Magic. It does NOT hold reachably for third casters (Eldritch Knight, Arcane Trickster) — their slot table lives in `subclassTableGroups` on the SUBCLASS entry, keyed to a non-caster base class (Fighter/Rogue), a shape slice (b)'s `spellSlots.ts` does not read. slice (b) returns no `SpellSlotsEntry` at all for an EK/AT character today, so this filter cannot cap them correctly yet — see docs/REPORT.md, this is a decision needed (does slice b grow a second read path, or does d1/the picker special-case subclass-held slots) before EK/AT support can be added. Every other spell field (level, ritual, concentration, viaVariant) passes through unchanged. No counts (max cantrips/prepared/known) — that's slice (d2). D11: takes one class at a time, no multiclass union.

- Investigation script — scripts/investigate-third-caster-slots.js, confirms exactly where Eldritch Knight/Arcane Trickster keep their spell-slot table ahead of teaching spellSlots.ts a second read path (D46): `subclassTableGroups` (not `classTableGroups`) on the subclass entry itself, `rowsSpellProgression` inside it in the identical `number[][]` shape the base-class reader already parses — same shape, different nesting only. Both subclasses carry `casterProgression: "1/3"`, a value spellSlots.ts's `CasterProgression` type does not yet include. Confirms these two are the only subclasses in the data carrying their own slot table (114 subclass entries checked). See docs/REPORT.md for the full findings.

- Spell counts (build order step 6, slice d2) — `src/calculation/spellCounts.ts`. Pure (D38), mirrors spellSlots.ts's shape exactly, including the D46 subclass fallback for Eldritch Knight/Arcane Trickster. `computeSpellCounts` returns `Calculated<SpellCountEntry[]>`, one entry per casting class in `character.classes` with `cantripCount`, `leveledSpellCount`, and a `label` (`'known'` or `'prepared'`) — the task's own instruction: prepared vs known casters and Wizard's spellbook are ONE picker, the difference is a label only. Investigated first (scripts/investigate-spell-counts.js, scripts/investigate-spell-counts-2.js): classes.json's `cantripProgression`/`preparedSpellsProgression` are already flat per-character-LEVEL numeric arrays with no ability-modifier assumption to add — the 2024 rules replaced the 2014 "ability modifier + level" formula with an explicit number per level in the class table itself (confirmed by reading Cleric's full array: it does not reduce to `level + a constant WIS modifier`). So the task brief's assumption that Cleric/Druid/Paladin/Artificer need the final ability score does NOT hold against the real XPHB data — every class's count is read structurally, per the task's own fallback rule ("if the data expresses the count structurally, read it"). `preparedSpellsChange` doubles as the label: `'restLong'` classes (Cleric, Druid, Paladin, Ranger, Artificer, Wizard) → "prepared"; `'level'` classes (Bard, Sorcerer, Warlock, Eldritch Knight, Arcane Trickster) → "known".
- Typed loaders for real classes.json — `src/spells/spellSlotsClassData.ts` and `src/spells/spellCountClassData.ts`. Neither existed before this task (STATUS previously noted "no loader wires this data in yet" for spell slots); both do the raw-JSON-to-typed-slice conversion spellSlots.ts/spellCounts.ts themselves never do (D38). The slot loader finds a class's ordinary-slots table by `rowsSpellProgression` and its Pact Magic table by column NAME (`colLabels` containing "Spell Slots"/"Slot Level", never assumed to be a fixed position); both attach a subclass's own table (D46) only when the base class has none.
- Class spell picker (build order step 6, slice d2) — `src/spells/SpellPicker.tsx`, wired into the wizard as a new `spells` step (between `abilities` and `featAsi`). ONE picker for every class (confirmed with the user: no separate prepared/known/spellbook mechanics, no slot-spending — that's step 9). Fetches the class's spell list (slice c) itself and applies the level filter (slice d1) against a `spellSlots`/count entry the wizard computes once via the calculation layer (`computeSpellSlots`, `computeSpellCounts`) against a draft Character — reused unchanged, never recomputed. Cantrips and leveled spells are counted and capped SEPARATELY (two independent running totals). A variant-sourced spell (slice c's `viaVariant` flag) is shown with a "(variant)" annotation but stays selectable, never hidden (DATA.md's trap). Ritual/concentration flags are shown per spell. The step is skipped entirely for a non-caster class (same `visibleSteps` mechanism the `expertise`/`featAsi` steps already use) — an Eldritch Knight/Arcane Trickster DOES see it once the third-caster slot fix (D46) gives them slots and this task's count loader gives them counts.
- Storage — `Character.spellChoices` (`src/storage/character.ts`), one entry per class (D11, same reasoning as `optionalFeatureChoices`) carrying `className`/`classSource` plus each pick's `name`/`source` — the picker's own `level` field (used for its in-wizard count checks) is dropped before saving, since it's re-derivable from spells.json (D4, sheet display, will re-look it up). Schema version bumped to 10 (from 9); a version-9 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav"). `wizardState.ts` gained the `spells` step, `WizardData.spellChoices`, and a `SpellRequirement` (cantrip/leveled counts + label) threaded through `visibleSteps`/`isStepComplete`/`isReadyToSave`/`saveCharacter` the same way `expertiseRequiredCount`/`featAsiEligibleLevelCount` already are. Clears whenever class, level or subclass changes (subclass matters because EK/AT eligibility depends on it).

- Subclass always-prepared spells (build order step 6, slice d2b) — `src/spells/subclassPreparedSpells.ts`. Not calculation (same reasoning as classSpellListData.ts, slice c — a data query, outside `src/calculation/`). `extractSubclassAlwaysPreparedSpells` is a pure function (D38): takes ONE subclass identity (name/source/className/classSource, matching `SubclassOption` from subclassData.ts) plus the character's level in that class, and the parsed classes.json/spells.json arrays; returns the subclass's always-prepared spells granted at or below that level. DERIVED, not stored (confirmed with the user) — no new Character field, no wizard state beyond passing the already-stored subclass/level through. D62: only the `prepared` shape of `additionalSpells` is read; a subclass using only one of the other 13 shapes returns nothing, cleanly. Shape confirmed first (D46, scripts/investigate-prepared-shape.js) against all 48 `prepared`-carrying additionalSpells entries: always an object keyed by class level, each value an array of spell references (bare lowercase name, or "name|source", occasionally with a stray "#..." tag stripped before matching) — 45 of 114 subclasses (72 with any additionalSpells) carry this shape; the other 27 additionalSpells-carrying subclasses are fully deferred. ONE data-shape variation the D46 read turned up that D62's note didn't anticipate: Bard College of Lore's level-6 `prepared` entry nests a choice object (`{choose: "level=0;1;2;3|class=..."}`) instead of a spell name — a pick from other classes' lists, not a fixed grant. Not handled — reported here and in this session's REPORT.md rather than invented; a non-string item in a `prepared` level's array is skipped cleanly, same mechanism as any other unhandled shape (D43-style). Two of 406 real spell references also don't resolve against this app's filtered spells.json (e.g. "branding smite") and are skipped the same way. Returned spells carry `origin: 'subclass'` and are explicitly separate from `Character.spellChoices` — they never add to or subtract from the class picker's (slice d2) cantrip/leveled counts. Minimal wiring: `AlwaysPreparedSpellsList.tsx` shows the list read-only inside the wizard's existing `spells` step, next to `SpellPicker`, whenever a subclass is chosen — this is display only, not the sheet (slice d4, still pending).

- Subclass always-prepared spells, `known`/`innate` follow-up (build order step 6, slice d6a) — extends `subclassPreparedSpells.ts` in place (no second module). `extractSubclassAlwaysPreparedSpells` now reads `known` and `innate` alongside `prepared` (all three are fixed-grant, level-keyed maps of the same shape), and correctly returns every fixed key a subclass mixes (e.g. Grave Domain's `prepared`+`known`) without dropping or doubling. Scope confirmed first (scripts/investigate-d6a-shapes.js, scripts/investigate-d6a-other-shapes.js, D46) against the 35 subclasses slice d2b left classified as "fixed grant, other key": 5 of them (Artificer Alchemist [EFA], Barbarian Path of the Wild Heart, Fighter Psi Warrior, Warlock The Fathomless, Monk Way of the Sun Soul/Warrior of Shadow) wrap a level's spell list one nesting deeper under a `resource`/`daily`/`ritual` key holding a per-day/ritual usage limit this app tracks nowhere else — `extractRefs` unwraps recursively and returns those spells as plain grants, discarding the usage-limit key. `expanded` is deliberately never read by this module: it hides three unrelated things — Eldritch Knight/Arcane Trickster/Divine Soul use it to widen the class spell picker's offered pool (a d2-picker change, not a grant, and not this slice); three Warlock patrons (Celestial, Hexblade, Fathomless) grant fixed patron-boon spells under it but keyed by pact slot RANK ("s1".."s5") rather than class level, needing the Pact Magic slot table to translate — deferred, decided with the user this session, not built; Warlock The Genie has 4 separate additionalSpells entries (one per genie kind: Dao/Djinni/Efreeti/Marid) and nothing in this app stores which kind the player chose — deferred, same reason. Where a subclass mixes `expanded` with a genuine fixed `known` grant (Arcane Trickster, Divine Soul), only the fixed part comes through; EK (no fixed key at all) still returns nothing. Warlock Archfey Patron keys its one innate grant `"_"` instead of a class level — `Number("_")` is not finite, so the existing level-gate check already skips it with no special case. No sheet change: `CharacterSheet.tsx` already renders whatever this function returns, so the newly-handled grants appear automatically.

- Investigation script — scripts/investigate-feat-spells.js, classifies the 29 (of 128) feats carrying `additionalSpells` ahead of slice d5: 4 fixed grants (derived lookup only, like d2b/d6a) and 25 player-choice grants (need a picker + storage, like d2). See docs/REPORT.md for the full breakdown and open questions this raised.
- Investigation script — scripts/investigate-feat-spell-ability.js, classifies the same 29 feats' `ability` field ahead of slice d5: 7 name a fixed ability, 17 offer a choice among int/wis/cha (Magic Initiate's shape), 5 give no real ability (4 literal "inherit", plus Ritual Caster which has no `ability` key at all). Of the 4 fixed-SPELL feats (Drow High Magic, Fey Teleportation, Telekinetic, Telepathic), 2 also have a fixed ability and 2 say "inherit" — so a fixed-spell feat is not automatically a pure derived lookup for ability. See docs/REPORT.md for the full breakdown.
- Sheet spellcasting display (build order step 6, slice d4). `CharacterSheet.tsx` gains three new sections, shown only when the character has at least one casting class (nothing renders for a non-caster, no empty headers): "Spellcasting" (spell attack bonus + spell save DC, one entry per casting class per D11, each a number with a `ValueBreakdown`), "Spell slots" (ordinary slots 1-9 as static per-level numbers, Warlock Pact Magic shown as its own count+slot-level block, never merged into the 1-9 list), and "Spells" (`SpellList.tsx`, new) — the player's chosen spells (`Character.spellChoices`) and the subclass's always-prepared spells (`subclassPreparedSpells.ts`, slice d2b) merged into ONE list grouped by spell level (cantrips first), an overlap counted once with both sources named (`combineSpellEntries`, the D44 "counted once, sources joined" spirit). Each spell is a collapsed `<details>` (feat-list pattern); expanding shows Casting Time/Range/Components/Duration/Attack-or-Save/Source (read from spells.json's own structured fields via the new `src/spells/spellDetailData.ts` + `src/sheet/spellFormatting.ts`, never hand-formatted prose) and the description through `ResolvedEntries` (D51), so a ref* inside a spell's text expands the same way a feat's does. `sheetData.ts` gained `loadSpellcastingAbilityClassData` (classes.json's `spellcastingAbility` field) and `resolveSubclassSource`/`loadSubclassSource` — see the subclass-source note below.

  Subclass source resolution (a decision taken this session, not a silent choice): `Character.classes[].subclass` stores only the subclass's NAME — `wizardState.ts`'s `saveCharacter` has always dropped the source it held at selection time (`SubclassChoice.source`), an existing gap outside this task's "no wizard changes" scope. The always-prepared-spells lookup needs the source. `scripts/investigate-sheet-subclass-source.js` found 11 of 103 (class, subclass name) pairs in classes.json are genuinely ambiguous by name alone (e.g. Artificer "Alchemist" exists under both TCE and EFA with different `additionalSpells` content — not a reprint). Resolved with the user: match the subclass entry whose own `classSource` equals the character's class's `classSource` first; if more than one candidate still matches (true for all 11 ambiguous pairs, since both candidates already share the class's `classSource`), resolve to unresolved rather than guess — the sheet then shows no always-prepared spells for that class rather than risk the wrong sourcebook's list (D43-style, not a silent wrong answer). The other 103 pairs resolve correctly and unambiguously.

- Spellcasting ability, third-caster follow-up (D46) — closes the gap slice d4 flagged above. `spellcasting.ts`'s `computeSpellcasting` now also reads a subclass's own `spellcastingAbility` (Eldritch Knight/Arcane Trickster), the same subclass-fallback pattern `spellSlots.ts`/`spellCounts.ts` already use: `ClassSpellcastingAbility` gained an optional `subclasses: SubclassSpellcastingAbility[]`, only consulted when the base class itself has no ability. `sheetData.ts`'s `extractSpellcastingAbilityClassData` populates it from classes.json, gated on the subclass ALSO carrying its own spell-slot table (not just the ability field) — `scripts/investigate-subclass-spellcasting-ability.js` found a third subclass, Path of the Ancestral Guardian (Barbarian), carries its own `spellcastingAbility` (WIS) but no slot table at all; per the user's decision this session, it stays excluded (no slots means nothing to cast, so an attack bonus/DC there would be a meaningless number) — only EK/AT get an entry. The sheet needed no code change: `CharacterSheet.tsx` already renders whatever `computeSpellcasting` returns, so an EK/AT character now shows the Spellcasting section alongside its already-working Spell slots/Spells sections.

- Investigation script — scripts/investigate-feat-spell-choice-shapes.js, groups the 27 remaining choice-spell feats (25 with a `choose` node + Telekinetic/Telepathic, which turn out to carry no spell choice at all) by the structure of what the player picks, ahead of slicing d5b into pickers. See docs/REPORT.md for the full grouping.
- Investigation script — scripts/investigate-class-feature-choices.js, build order step 6a's first task (D63): inventories selectable class/subclass features ahead of any picker/storage/wizard/sheet work (none of that is built by this task). Confirms only 2 base classes carry CLASS-level optionalfeatureProgression (Sorcerer Metamagic, Warlock Eldritch Invocations) — both structurally readable, but the `progression` field's own SHAPE differs between them (Sorcerer: sparse object keyed by class level, same shape subclass progressions already use; Warlock: a dense array 0-indexed by level-1) — a reader written for one silently breaks on the other, same class of trap as weapon mastery (DATA.md). Warlock's classTableGroups "Invocations Known" column agrees with the progression once read shape-correctly. Confirms the existing subclass picker's 4 codes (FS:B/AS/RN/MV:B) are still the only subclass-level optionalfeatureProgression entries — nothing new for it to pick up for free. All 6 featureType codes found (MM/EI/FS:B/AS/RN/MV:B) resolve to a non-zero option count. Of the 102 distinct resolved options, 52 carry a `prerequisite` field across 7 shapes; 6 of those name another option from the same resolved set by name inside an `optionalfeature` prerequisite key (e.g. Eldritch Smite requires Pact of the Blade, itself an invocation) — a same-wizard-choice ordering dependency no picker has had to handle yet. Separately, 20 class/subclass features (5 + 15) carry a D21 `options` text node no picker currently reads — mostly `refClassFeature`/`refSubclassFeature`/`refOptionalfeature` pointers to alternate feature text (e.g. Cleric's Divine Order, Druid's Primal Order — pick-one-of-two class features, not the Invocations/Maneuvers style at all). Effect fields (ability, skillProficiencies, etc.) are absent from every option entry found except `additionalSpells` (17) and `senses` (3, always truesight/blindsight) — see docs/REPORT.md for what this means for scoping the picker(s).
- Eberron "Mark of ..." feats' fixed spell grants (build order step 6, slice d5b-3) — extends `src/spells/featSpells.ts` in place (no second module, same style as d5a). The 12 marks turned out to carry a genuinely fixed spell grant (`prepared`/`known`, no `choose` node inside — confirmed by a short re-read this task) alongside a CHOICE ability (int/wis/cha) and a separate `expanded` pool-widening list. `extractFixedFeatSpells` now resolves ability two ways: a plain named ability (Drow High Magic/Fey Teleportation, unchanged) OR, ONLY for a feat named "Mark of ...", the character's already-stored D57 `chosenAbility` — guarded by name so this doesn't also sweep in Magic Initiate's or Boon of Siberys's choice-ability entries (Boon of Siberys is an array of 13 full alternatives the player must pick ONE of; applying the mark logic there unconditionally would incorrectly grant all 13). The mark's spell is granted even if `chosenAbility` isn't recorded yet — `FeatGrantedSpell.ability` is now optional, `undefined` rather than invented, when there's nothing to carry. A mark's numerically-keyed grant (e.g. `prepared["3"]`, gust of wind for Mark of Storm) is now gated on the character's TOTAL level (D11, summed across `character.classes` — a feat has no single class to gate on); a "_"-keyed grant (every d5a feat, and each mark's base spell) is always granted regardless of level — this same level-gating loop replaces d5a's old direct `extractRefs(grant)` call with no behavior change for Drow High Magic/Fey Teleportation, since both only ever used "_". `expanded` is still never read (deliberately deferred, grouped with the subclass `expanded` deferrals — see docs/REPORT.md). No sheet code change: `SpellList.tsx` already renders whatever `featSpells.ts` returns.
- Base Magic Initiate picker (build order step 6, slice d5b-2). Shape confirmed via `scripts/investigate-magic-initiate-shape.js`: base "Magic Initiate" (XPHB) carries 3 named `additionalSpells` alternatives ("Cleric Spells"/"Druid Spells"/"Wizard Spells"), each with a choice ability (int/wis/cha) and a "known" (2 cantrips) + "innate" (1 level-1 spell, capped at level 1 by the feat's own rule) grant. `featAsiData.ts`'s `extractSelectableFeats` now hides the three pre-selected "Magic Initiate; Cleric/Druid/Wizard" variants (XPHB) and Boon of Siberys (EFA, deferred — its `additionalSpells` is 13 full named alternatives, a distinct picker shape not built this slice) — one `HIDDEN_FEAT_KEYS` set, commented as revisitable (D23-style). `FeatAsiPicker.tsx`'s new `MagicInitiateSubPicker` renders once base Magic Initiate is chosen: a class-list radio (Cleric/Druid/Wizard, hardcoded — matches the feat's own 3 named alternatives, not re-parsed from `additionalSpells`), then that class's cantrips/level-1 spells via `classSpellListData.ts` (slice c), counts enforced (exactly 2 + 1, SpellPicker/d2's toggle pattern), then the ability choice reusing the existing `chosenAbility` slot (D57) with its own narrower int/wis/cha option set. Choosing a different class list clears the previous list's picks (a fresh object, same as switching feats). Storage: `FeatAsiChoice`'s `kind: 'feat'` branch gained an optional `magicInitiate: { className, classSource, cantrips, spell }` (storage/character.ts); schema version bumped to 11 (from 10) — a version-10 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav"). `wizardState.ts`'s `isCompleteFeatAsiChoice` requires a full pick (class + 2 cantrips + 1 spell + ability) before the `featAsi` step completes for this one feat, name-guarded the same way the mark-spell logic in featSpells.ts is. Sheet: `featSpells.ts`'s `extractFeatGrantedSpells` gained `extractMagicInitiateSpells`, which reads the character's OWN stored `magicInitiate` pick directly (not `additionalSpells` — the player chose the individual spells, there's no fixed grant to derive) and looks up level/ritual/concentration from spells.json; `SpellList.tsx` needed no change, the "from feat (Magic Initiate)" label falls out of the existing `featOrigins` provenance path.
- Generic filter-choice feat spell picker (build order step 6, slice d5b-1 — the LAST feat-spell picker, closes d5b). `scripts/investigate-filter-choice-picker-shape.js` confirmed the shared shape ahead of writing anything: all 8 in-scope feats' `choose` nodes are one string mini-language, `"level=N|<filter>"`, nested under `known` (always a cantrip, N=0) or `innate`/`prepared` (always a level-1 spell, N=1); `<filter>` is `class=<Name>` (case varies — Blessed Warrior/Druidic Warrior use lowercase), `school=<Code>;<Code>` (spells.json's own single-letter school code, matched directly), or the literal `components & miscellaneous=ritual` (Ritual Caster). `investigate-feat-spell-choice-shapes.js`'s prior 11-feat grouping includes 3 feats.json entries ("Magic Initiate; Cleric/Druid/Wizard") that are permanently hidden from the feat picker (featAsiData.ts `HIDDEN_FEAT_KEYS`, base Magic Initiate/d5b-2 already replaces them) and so can never be chosen — no picker was built for them, shrinking this slice's real scope to 8 feats: Artificer Initiate, Blessed Warrior, Druidic Warrior, Wood Elf Magic, Aberrant Dragonmark (class-list filter), Fey-Touched, Shadow-Touched (school filter), Ritual Caster (ritual-tag filter). New module `src/spells/featSpellChoiceData.ts`: `filterChoiceFeatShape` parses a feat's `known`/`innate`/`prepared` trees into up to two slots (`cantripSlot`, `spellSlot`), each carrying its filter and pick count; `offeredSpellsForSlot` returns the guided candidate list (class slot reuses classSpellListData.ts, school/ritual slots read spells.json directly); Ritual Caster's `count` is deliberately NOT read from the data — the 5 level-keyed cumulative counts (2 at level 1, +1 at 5/9/13/17) exactly track `proficiencyBonusForLevel` at those same thresholds (confirmed by the investigate script), so `ritualCasterSpellCount(totalCharacterLevel)` computes it instead; `filterChoiceRequiredCounts` is a small hardcoded table (same style as `MAGIC_INITIATE_CLASS_OPTIONS`) so the wizard's synchronous step-completeness check doesn't need to re-parse feats.json. `FeatAsiPicker.tsx`'s new `FilterChoiceSpellSubPicker` renders once one of the 8 feats is chosen: no class-list radio (unlike Magic Initiate, each of these 8 feats' filter is fixed by the feat itself), just the offered cantrip/spell checkboxes per slot with counts enforced, plus any fixed companion spell (Fey-Touched's Misty Step, Wood Elf Magic's Longstrider/Pass without Trace) shown as already granted via `featSpells.ts`'s new `loadFixedFeatSpells` wrapper. Ability: the 3 feats with a stored-choice ability (Fey-Touched, Shadow-Touched, Ritual Caster) already have a normal half-feat `ability` field in feats.json, so `chosenAbility` (D57) populates through the EXISTING ability picker with no new wiring; the other 5 have a feats.json-fixed ability, read as before. Storage: `FeatAsiChoice`'s `kind: 'feat'` branch gained an optional `filterChoiceSpells: { cantrips, spells }` (storage/character.ts, plus matching validate.ts checks); schema version bumped to 12 (from 11) — a version-11 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav"). `wizardState.ts`'s `isCompleteFeatAsiChoice` now also blocks the `featAsi` step until a filter-choice feat's picks reach `filterChoiceRequiredCounts`. Sheet: `featSpells.ts` gained `extractFilterChoiceSpells` (reads the stored pick directly, same pattern as `extractMagicInitiateSpells`) plus a small `extractFixedFeatSpells` extension so Fey-Touched/Shadow-Touched's fixed companion spell (previously skipped — their `additionalSpells.ability` is the literal string `"inherit"`, which no existing branch resolved) now also reaches the sheet, using `chosenAbility` per this slice's decision rather than the "derives from existing spellcasting ability" reading of "inherit". `SpellList.tsx` needed no change — the "from feat (Name)" provenance path already covers both halves.
- Fixed feat-granted spells (build order step 6, slice d5a) — `src/spells/featSpells.ts`. Same reasoning as classSpellListData.ts/subclassPreparedSpells.ts (a data query, outside `src/calculation/`), and reuses subclassPreparedSpells.ts's `extractRefs`/`parseSpellRef`/`findSpell`/`isRawSpell`/`hasConcentration`/`isRecord` (now exported) rather than a second copy — feat `additionalSpells` is the same field shape subclasses use (D46). Handles ONLY the 2 feats (of 29 spell-granting feats found by scripts/investigate-feat-spells.js) that are fixed in BOTH spell and ability: Drow High Magic (CHA) and Fey Teleportation (INT) — scripts/investigate-fixed-feat-spell-shape.js confirmed the exact shape (`{"ability":"cha","innate":{"_":{"will":[...],"daily":{"1e":[...]}}}}`). The other 27 (25 choice-spell feats, plus Telekinetic/Telepathic whose spells are fixed but ability is literal "inherit") are slice d5b, a picker — not read here at all: `extractFixedFeatSpells` only proceeds when `ability` is a plain named ability code (excludes both a `choose` object and the literal string "inherit"). Unlike subclass grants, a feat has no class-level gate to apply — it's granted outright once taken (`Character.featAsiChoices`) — so `innate`'s "_" wrapper key (not a class level) is read directly via `extractRefs` rather than skipped the way subclassPreparedSpells.ts skips a non-numeric level key. Returned spells carry `origin: 'feat'` and `featName` (the "from feat (Name)" provenance the user asked for, distinct from subclass's "always prepared (subclass)" label) plus the FIXED `ability` for a later attack/DC consumer — attack/DC computation itself is NOT built this task (see docs/REPORT.md). Additional to the class picker's own counts, same as subclass grants. Wired into the sheet: `SpellList.tsx`'s `SheetSpellEntry` gained `featOrigins` (parallel to `subclassOrigins`), `combineSpellEntries` takes feat-granted spells as a third source, `CharacterSheet.tsx` loads them via `loadFeatGrantedSpells(character)` (depends on `character.featAsiChoices`, fetched separately like the subclass lookup).

- Sheet fix: feat-granted spells now show for non-casters. `CharacterSheet.tsx`'s "Spells" section (`SpellList.tsx`) was gated on `isCaster` (having a casting class) alongside "Spellcasting" and "Spell slots" — a non-caster with a spell-granting feat (e.g. Fighter + Magic Initiate) had a genuinely non-empty spell list but no casting class, so the whole block, including the list, was hidden. The Spells section is now gated on `combinedSpells.length > 0` instead, independent of `isCaster`; "Spellcasting" (attack bonus/DC) and "Spell slots" stay gated on `isCaster` as before, since a pure non-caster genuinely has neither. A non-caster's feat spells therefore show in the list with no attack/DC and no slots — computing attack/DC for a feat-only caster is a standing deferred gap (docs/REPORT.md), not built this task.
- Sheet fix: a filter-choice feat's player-CHOSEN spells (d5b-1 — Fey-Touched's picked school spell, Ritual Caster's picked rituals, etc.) now actually reach the sheet. The extraction (`extractFilterChoiceSpells`) and its inclusion in `extractFeatGrantedSpells`/`loadFeatGrantedSpells` were already correct and already tested (featSpells.test.ts) — the real bug was upstream, in `FeatAsiPicker.tsx`'s shared half-feat ability `<select>` (used by Fey-Touched/Shadow-Touched/Ritual Caster/Aberrant Dragonmark, which all have a normal `ability` choice field alongside their spell choice, per D57): its `onSelectAbility` callback rebuilt the stored `FeatAsiChoice` object from only `{level, kind, name, source, chosenAbility}`, silently dropping any already-recorded `filterChoiceSpells` (and `magicInitiate`) whenever the ability was set or changed AFTER the spell pick — e.g. player picks the spell first, then picks the ability last, a perfectly natural order since both controls are visible from the moment the feat is selected. `MagicInitiateSubPicker`'s own ability callback already did this correctly (`{ ...current, chosenAbility: ability }`); `onSelectAbility` at `FeatAsiPicker.tsx` now matches that pattern. No storage-format change, no new picker — confirmed via a manual localStorage-injected character that the extraction/display path itself was already correct once the data survives the picker intact.

- Subclass filter-choice spell picker (build order step 6, slice d6b — the LAST picker of step 6). `scripts/investigate-d6b-choice-shapes.js` confirmed the 5 subclasses' exact `choose` shapes ahead of writing anything: Bard College of Lore grants 2 spells at class level 6 in ONE step, each a `{choose: "level=0;1;2;3|class=Cleric;Druid;Wizard"}` node (another class's list, level <= 3); the 4 core Wizard subclasses (Abjurer/Diviner/Evoker/Illusionist) instead grant progressively, under `known`: 2 nodes at level 3 (cap level 0-2), then 1 more node each at levels 5/7/9/11/13/15/17 (cap growing by 1 spell level per node, always the subclass's OWN school — "school=A/D/V/I") — 9 slots total per subclass, each carrying BOTH a class clause (`class=Wizard`) AND a school clause together, not one or the other. This didn't fit d5b-1's mini-language parser unchanged (a feat's `choose` string never combined a class clause with a school clause, and never listed more than one spell level) — `src/spells/featSpellChoiceData.ts` was generalized in place rather than copied (task instructions): `SpellChoiceSlot.level` became `levels: number[]`, `SpellChoiceFilter`'s class kind became `classes: {className,classSource}[]` (College of Lore's 3-class list) with an optional `schools?: string[]` (the Wizard subclasses' combined class+school filter), and `offeredSpellsForSlot` now takes the minimal `{levels, filter}` shape so a second module can reuse it directly. New module `src/spells/subclassSpellChoiceData.ts`: `subclassSpellChoiceShape` parses a subclass's `known`/`prepared` trees into an ordered list of slots, each identified by `(grantedAtLevel, slotIndex)` rather than array position (so a stored pick stays self-describing regardless of order) — `slotIndex` disambiguates the 2 simultaneous slots at Lore's level 6 and each Wizard subclass's level 3. `unlockedSubclassSpellChoiceSlots` filters to the slots granted at or below the character's class level. `SubclassSpellChoicePicker.tsx` renders one `<select>` per unlocked slot (not one shared checkbox list like the feat picker — the Wizard subclasses' slots have DIFFERENT level caps active simultaneously, so each slot must offer only its own candidates), wired into the wizard's existing `spells` step alongside `SpellPicker`/`AlwaysPreparedSpellsList`, shown only for the 5 covered subclasses (`isSubclassSpellChoice`) and only once a slot is unlocked (renders nothing below the grant level, e.g. College of Lore below character level 6). Storage: `Character.subclassSpellChoices` (`CharacterSubclassSpellChoice[]`, one entry per class carrying one of the 5 subclasses, D11) — `picks: CharacterSubclassSpellChoicePick[]`, each `{grantedAtLevel, slotIndex, name, source}`. Clears whenever class, level or subclass changes (`wizardState.ts`, same trigger points `spellChoices`/`optionalFeatureChoices` already use); the 'spells' step's completion check now also requires `subclassSpellChoices.length` to match the unlocked slot count, computed synchronously in `CharacterWizard.tsx` via a new effect (same pattern `featAsiGrantCount`/`expertiseEligibility` already use) and threaded through `isStepComplete`/`isReadyToSave`/`saveCharacter` as a new trailing parameter. Schema version bumped to 13 (from 12) — a version-12 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav"). Sheet: `subclassSpellChoiceData.ts`'s `extractSubclassChosenSpells` resolves the stored picks into the SAME `AlwaysPreparedSpell` shape a fixed subclass grant uses (`origin: 'subclass'`, `grantedAtLevel` read straight off the stored pick) — `CharacterSheet.tsx`'s existing subclass-spell effect merges these into the same per-class group `subclassPreparedSpells.ts`'s fixed grants already populate, so they reach `SpellList.tsx`'s existing "always prepared (subclass name)" provenance label with no new label invented and no change to `combineSpellEntries`. A dedicated sheet test (`CharacterSheet.test.tsx`) asserts a chosen Evoker spell actually renders with that label — this is the exact "picker saves but never displays" gap a prior slice (d5b-1's sheet fix) hit, closed here by testing the display path explicitly rather than assuming no change was needed. A fixed-grant subclass (e.g. Grave Domain) is unaffected — `subclassSpellChoiceShape` returns no slots for it, confirmed by a regression test.

- EK/AT class spell picker pool-widening (build order step 6, slice d6c — EK/AT `expanded` wiring, a blocker fix). Eldritch Knight and Arcane Trickster reach the wizard's `spells` step with a real `SpellSlotsEntry`/count entry (D46's third-caster fallback already wired both), but their class spell picker asked `classSpellListData.ts` for THEIR OWN (Fighter's/Rogue's) spell list — always empty, since neither class has one — so the picker offered 0 spells and the character could never complete creation. Their subclass entry's `additionalSpells.expanded` names Wizard's list as the pool to widen into (confirmed via scripts/investigate-ek-at-expanded.js: both key `{"all": "level=N|class=Wizard"}` under `expanded`, N tracking the same third-caster max-spell-level progression the existing slot table already encodes — no separate level cap to parse). `classSpellListData.ts` gained `spellListClassFor(className, classSource, subclassName)`: a small lookup table (`THIRD_CASTER_SPELL_LIST`, keyed by subclass name) returning `{className: 'Wizard', classSource: 'XPHB'}` for these two subclasses, the class's own identity otherwise. `CharacterWizard.tsx` calls it once to compute the identity passed to `SpellPicker`, instead of always passing the character's own class — no change to `SpellPicker.tsx`, the level filter, or the counts, which already worked once given the right list. `Character.spellChoices` still tags picks with the character's OWN class (Fighter/Rogue), matching `saveCharacter`'s existing D11 tagging — only which list is OFFERED changed, not how a pick is stored or how the sheet resolves it (`SpellList.tsx` looks up spell details by name/source only, never by class). ONLY these two subclasses are wired; the other `expanded` cases (Divine Soul, the 3 pact-slot-rank patrons, The Genie, the 12 marks) are unaffected and stay deferred, since none of them are blocked (Divine Soul already has the Sorcerer list; the others are fixed-grant or unmodeled-choice cases, not empty pools) — see docs/REPORT.md.

- Feat-spell attack bonus/save DC (build order step 6, closes the last standing sheet gap noted at d5a/d5b). `spellcasting.ts` gains `computeFeatSpellcasting`, the same attack-bonus/DC arithmetic as `computeSpellcasting` (ability modifier + proficiency bonus; DC also +8 base) applied to a new source — PER FEAT (D11's "one entry per source" pattern, same as one entry per casting class) rather than one blended value, so a character with Magic Initiate (CHA) and Fey-Touched (WIS) gets two entries. Takes the already-computed `FeatGrantedSpell[]` (`featSpells.ts`) and reads each feat's already-resolved `ability` straight off it — never re-derives or re-asks; a feat whose ability isn't resolved yet (a chosen-ability feat with no `chosenAbility` stored) returns the whole result `unknown` (D43), the same abort-on-missing-data behaviour `computeSpellcasting` already has for a missing class. `CharacterSheet.tsx`'s existing "Spellcasting" section now also lists a `<li>` per feat, styled identically to a class entry (`{Feat Name} (Ability)`, attack/DC with breakdown on expand) — the section's visibility check (`isCaster`) now also fires on a feat spellcasting entry alone, so a non-caster with a spell-granting feat (Fighter + Magic Initiate) shows a Spellcasting entry with no casting class and no slots section, closing the gap the d5a/d5b-1 sheet fix note left open. No storage change, no new picker.

- Investigated a reported bug (build order step 6, follow-up): "a Fighter with Magic Initiate shows its feat spells on the sheet but no Spellcasting section." Did NOT reproduce against the code above, on three independent checks: the calculation layer directly (`extractFeatGrantedSpells` piped into `computeFeatSpellcasting`), a hand-built but schema-correct character injected into `localStorage` and opened in a real browser, and a character built through the actual creation wizard end-to-end (class → species → background → languages → abilities → Magic Initiate picked at the level-4 feat step → sheet) — all three show the "Magic Initiate (Intelligence)" entry with the correct attack bonus and save DC. What WAS a real gap: every existing test fed `computeFeatSpellcasting` a hand-typed `FeatGrantedSpell` with `ability` set directly by the test author (`spellcasting.test.ts`'s `featSpell()` helper, and `CharacterSheet.test.tsx`'s mocked `loadFeatGrantedSpells`) — none of them ran the REAL `featSpells.ts` extraction and piped its output into `computeFeatSpellcasting`, so a break in that specific seam could have gone undetected by "tests pass." `spellcasting.test.ts` now has a `computeFeatSpellcasting — fed the REAL featSpells.ts extraction` block closing that gap for both ability-resolution paths (Magic Initiate's chosen ability, Drow High Magic's fixed ability). No code changed in `spellcasting.ts`, `featSpells.ts`, or `CharacterSheet.tsx` — see docs/REPORT.md for the full investigation and the likely explanation for the original report.

- Spell "At Higher Levels" upcasting text (build order step 6, DISPLAY only — closes step 6 except the standing deferrals below). Confirmed the field shape first (scripts/investigate-spell-upcast-shape.js, D46): `entriesHigherLevel` (187/489 spells) is an array of entry objects, same shape as the spell's own `entries` — usually one `{type: "entries", name: "Using a Higher-Level Spell Slot", entries: [...]}` — so it renders through the SAME markup renderer already used for the description, no hand-formatting. `scalingLevelDice` (26/489, cantrip level-scaling) is a different, non-entries shape (a dice-by-level map) and was deliberately left out — it would need new formatting code, not just display, which is out of this task's scope. `spellDetailData.ts`'s `SpellDetail` gained `entriesHigherLevel: unknown[]`, populated the same way `entries` already is. `SpellList.tsx`'s expanded spell detail gains one more block, labelled "At Higher Levels", rendered via `<ResolvedEntries>` (D51, same component the description uses) — shown only when the array is non-empty, no empty label otherwise. No calculation, no new picker, no tie to a chosen slot level (that's step 9, slot spending).

- Cantrip character-level scaling display (build order step 6, DISPLAY only — the follow-up the upcasting task deferred, completes the spell-detail view). Confirmed the field shape first (scripts/investigate-scaling-level-dice-shape.js, D46) across all 26 `scalingLevelDice`-carrying spells: all 26 are cantrips (no leveled spell carries it), the field is either a single `{label, scaling}` object (23) or an array of them (3 — e.g. Booming Blade's two separate damage dice, one for moving and one for hitting) — normalized to always an array in `spellDetailData.ts`. `scaling` is a map of character-level threshold (stringified number key) to a dice string; thresholds vary per entry (Booming Blade's "on hit" entry starts at level 5, not 1). This is structured data, not entry text, so it's formatted directly rather than through the markup renderer (unlike `entriesHigherLevel`): `spellFormatting.ts`'s new `formatScalingLevelDice` turns each entry's threshold map into a readable range list (e.g. "Fire damage: 1d10 (1-4), 2d10 (5-10), 3d10 (11-16), 4d10 (17+)"), computing the range boundaries from the sorted threshold keys themselves — never a hardcoded 1/5/11/17. `SpellDetail` gained `scalingLevelDice: SpellScalingLevelDiceEntry[]`. `SpellList.tsx`'s expanded spell detail gains one more block, labelled "Cantrip scaling", one line per entry — shown only when the array is non-empty. No calculation tied to the character's own level; the full table is shown as reference, same principle as the upcasting text. This closes the spell-detail display started in slice d4.

- Bug fix: a spell reachable via two grant paths was emitted twice. The reported repro (Divine Soul Sorcerer, Bless showing twice under "Always prepared from Divine Soul") did NOT reproduce on investigation — `extractSubclassAlwaysPreparedSpells` returns Bless exactly once for Divine Soul, confirmed three ways (direct pure-function call against the real data in-browser, a hand-built character rendered on the sheet, and the full real wizard flow through class → species → background → languages → abilities → spells) — see docs/REPORT.md. A genuine instance of the SAME class of bug turned up while checking other subclasses for the same risk (per the task's own ask): College of Glamour (Bard, XPHB) lists "Command" under BOTH `prepared["6"]` and `innate["6"]` in the same `additionalSpells` entry (scripts/investigate-glamour-duplicate-2.js) — `extractSubclassAlwaysPreparedSpells`'s FIXED_GRANT_KEYS loop visited both keys and pushed it twice. Fixed generally, not as a Divine-Soul-specific patch: `subclassPreparedSpells.ts` gained `dedupeAlwaysPreparedSpells` (collapses by spell identity — case-insensitive name, uppercase source — keeping the LOWEST `grantedAtLevel` seen), applied at the end of `extractSubclassAlwaysPreparedSpells` itself (fixes the root cause AND the wizard's `AlwaysPreparedSpellsList.tsx`, which renders that function's output raw with no dedup of its own) and again in `CharacterSheet.tsx` where a subclass's fixed grant is concatenated with its d6b chosen picks (`[...alwaysPrepared, ...chosen]`) — defensive, in case a player's chosen pick ever coincides with the subclass's own fixed grant. `featSpells.ts` shares the identical FIXED_GRANT_KEYS loop shape, so the same risk exists there (no confirmed real instance among the feats checked, but nothing rules one out): gained its own `dedupeFeatGrantedSpells`, applied at the end of `extractFixedFeatSpells` (per-feat scope — also fixes `FeatAsiPicker.tsx`'s raw `fixedCompanions` listing) but deliberately NOT at `extractFeatGrantedSpells`'s cross-feat aggregate, since two DIFFERENT feats granting the same spell is a real case the sheet should show with BOTH feat names joined (already working correctly via `SpellList.tsx`'s `combineSpellEntries`, D44 spirit) — collapsing there would have deleted that provenance instead of joining it. Checked and confirmed already correct, no code change needed: a player-picked spell (`Character.spellChoices`) that coincides with a subclass or feat grant already shows ONCE on the sheet with joined provenance (e.g. "Bless — player pick; always prepared (Divine Soul)"), via `combineSpellEntries`'s existing name+source Map — this was verified both in-browser and with a new regression test.

- Class feature choices, data layer (build order step 6a, slice 1) — extends `src/optionalFeatures/optionalFeatureData.ts` in place (no parallel module), reusing its existing D12-aware featureType→options resolution. Pure functions and tests only: no picker, no storage field, no wizard step, no sheet section (slice 2).
  - `classOptionalFeatureGrantsFor(parsedClasses, className, classSource, level)` returns one `{featureType, count}` per code a CLASS's own `optionalfeatureProgression` grants. The `progression` field's SHAPE differs between the only two classes carrying one (Sorcerer MM: sparse object keyed by class level; Warlock EI: dense array 0-indexed by level-1), so `countFromProgression` branches STRUCTURALLY on `Array.isArray`, never on class name — the sparse branch reuses the existing `countAtLevel` the subclass reader already uses. A count of 0 is returned as an entry (Sorcerer at level 1 grants Metamagic, just not yet); a class with no progression at all returns an empty array. Guarded by a test that reads the real classes.json and asserts the progression-derived count equals Warlock's own classTableGroups "Invocations" column at every level 1-20.
  - `optionsForFeatureType(parsedOptionalFeatures, parsedFeats, code)` exposes the existing resolution (D12's `FS:*`→feats.json category "FS" fallback included) for a class-level code; `optionalFeatureChoicesFor` now calls it instead of its own inline copy. `OptionalFeatureOption` gained an optional `prerequisite` field, carried through from both resolution paths.
  - `evaluateOptionalFeaturePrerequisites(option, ctx)` / `evaluateOptionalFeatureOptions(options, ctx)` evaluate all 7 prerequisite shapes (combinations of 5 keys: `level`, `spell`, `pact`, `optionalfeature`, `feature`). Array = alternatives (OR), keys within one alternative AND'd — the same rule feats.json follows. Returns `{eligible, reasons[]}`, never a bare boolean, and `evaluateOptionalFeatureOptions` never drops an ineligible option (D19, the pattern the feat picker already follows). D38: `OptionalFeaturePrerequisiteContext` carries `classLevels`, `knownSpellNames`, `chosenOptions`, `availableOptions` and `hasFightingStyleFeature` — all passed in, nothing fetched, nothing read from character storage.
  - `featAsiData.ts`'s `evaluateFeatPrerequisites` was NOT reused: only the `feature` key overlaps (both are the single value "Fighting Style"), and `level` collides in key NAME while differing in value shape — feats.json's is a bare character-level number, an optional feature's is `{level, class:{name,source}}` (+`subclass` on 2 entries), i.e. a CLASS level. The other three keys (`spell`, `pact`, `optionalfeature`) do not occur in feats.json at all. The two evaluators share the OR/AND rule and the "Requires ..." reason wording by convention, not by code.
  - Two decisions taken with the user this session (scripts/investigate-pact-prereq.js): (1) a `pact: "X"` prerequisite resolves to the same-list invocation "Pact of the X" — XPHB models the pact boon as an invocation, so Blade/Chain/Tome are satisfied by a sibling choice; "Talisman" has no such option anywhere in this data, so the 3 TCE Talisman invocations are permanently ineligible with a reason that says so, never hidden. (2) `spell` prerequisites: the two literal ref forms are matched by name against the supplied known-spell list ("hex/curse#x" read as alternatives, the "#" tag stripped); the `choose` form ("a Warlock Cantrip That Deals Damage", 5 entries incl. Agonizing Blast) has no structural test the supplied input can answer and reports unmet with its own text. **Superseded in slice 2** for the 2 filters the context can now answer — see below.

- Class-level optional features, UI/storage/sheet (build order step 6a, slice 2) — Sorcerer Metamagic and Warlock Eldritch Invocations are now selectable, saved and displayed.
  - Picker — `src/optionalFeatures/ClassOptionalFeaturePicker.tsx`, FORKED from `OptionalFeaturePicker.tsx` rather than shared with it (that one has a single subclass-keyed count, a `string[]` value and no prerequisites; only the ResolvedEntries/Entries description fallback is common). One group per granted featureType, each with its own independent running total — the way SpellPicker counts cantrips and leveled spells separately. Ineligible options stay visible and disabled with their reasons (D19), including the 3 Talisman invocations. Prerequisites are re-evaluated on every change, so taking Pact of the Blade unlocks Eldritch Smite without a remount.
  - Removal case, decided: an already-chosen option that stops qualifying is NOT auto-dropped. It stays checked, carries a warning naming the unmet prerequisite, stays toggleable so the player can undo it themselves, and blocks its own step (`areClassOptionalFeaturesComplete`).
  - Agonizing Blast — `OptionalFeaturePrerequisiteContext` gained optional `damagingCantripsByClass` (class name → the character's own known cantrips whose spells.json `damageInflict` is non-empty; D21, structural, never a spell-name list). The evaluator's `choose` branch parses the filter string (`"level=0|class=Warlock"`) and is satisfied when the context can answer it; any further clause (Repelling Blast's `"spell attack=m;r;o"`) or a missing context keeps slice 1's "unmet, with the prerequisite's own text". `SpellDetail` gained `damageInflict` and `damagingCantripsAmong(details, known)` narrows a character's picks to damaging cantrips.
  - Storage — NO schema change. A class-level pick reuses `Character.optionalFeatureChoices` (`{featureType, choices}[]`) as-is; `saveCharacter` concatenates the class's entries onto the subclass's. Nothing distinguishes the two beyond the featureType code, and `chosenClassOptionalFeatures` tells them apart by asking whether the CLASS's own progression grants that code.
  - Wizard — `WizardData.classOptionalFeatureChoices` (already storage-shaped), cleared on `setClassChoice` (class OR level) but NOT on `setSubclass`: a class-level grant never waits for a subclass (D13). The step cannot advance until every granted count is filled and no pick is in the warning state. (Slice 2 put this on the class step, which ran BEFORE 'spells' and left Agonizing Blast reachable only by back-navigation; D64 moved it — see the follow-up bullet below.)
  - Sheet — one `<section>` per granted featureType headed by the progression's own `name` from classes.json ("Eldritch Invocations", "Metamagic"), one collapsed `<details>` per option rendered through `ResolvedEntries` (D51), exactly as the feat list does. A character with no class-level picks renders nothing — no empty heading. Covered by a test that builds a Warlock with chosen invocations and asserts they render.
  - Investigation script — scripts/investigate-damage-cantrip-prereq.js (D46): `damageInflict` is a string array, always non-empty when present (193/489 spells; 28/47 cantrips); only 3 options carry a `choose` spell prerequisite (not 5 as slice 1 reported) — Agonizing Blast and Eldritch Spear share one filter, Repelling Blast adds an attack-roll clause; both classes' `optionalfeatureProgression` entries carry a display `name`.

- Class-level optional features as their own wizard step (build order step 6a follow-up, D64) — the picker moved off the class step to a new `classOptionalFeatures` step placed directly after `spells`. Subclass-level optional features (Battle Master Maneuvers, Rune Knight Runes, Arcane Shot, College of Swords Fighting Style) are untouched and stay on the class step; D13 is superseded only for the class-level ones.
  - Visibility uses the same `visibleSteps` mechanism as `expertise`/`featAsi`/`spells`: a new trailing `classOptionalFeatureGroupCount` parameter (the length of `classOptionalFeatureGroupsFor`'s result, which already drops count-0 grants) threads through `visibleSteps`/`pickerSteps`/`stepIndex`/`nextStep`/`previousStep`/`isReadyToSave`/`saveCharacter` and both navigation actions. A class granting nothing at class level never sees the step and the numbering stays contiguous.
  - Completion gating moved with it: `isStepComplete`'s `class` case no longer reads `classOptionalFeaturesComplete` (it is back to name + class choice), and the new `classOptionalFeatures` case returns it. The picker itself is unchanged — same props, same D8 controlled-component shape, so navigating away and back preserves the picks.
  - Step label — `stepLabel()` in CharacterWizard.tsx reads the progression's own `name` already carried on `ClassOptionalFeatureGrant` ("Eldritch Invocations", "Metamagic"), joining them when a class grants more than one, and falls back to a generic "Class options" if a future class omits the name. No featureType-code-to-label map.
  - Tests — `src/creation/CharacterWizard.classOptionalFeatures.test.tsx` (6): a Warlock walked FORWARD with no back-navigation picks Eldritch Blast on the spells step and finds Agonizing Blast selectable on the next one (the point of D64, proven rather than assumed); the class step's Next is enabled with no invocation chosen; the step is named after the progression and sits directly after Spells; away-and-back preserves picks; the step blocks Next until every count is filled; a Sorcerer completes through to an enabled save; Fighter never sees the step and numbering stays contiguous. The prerequisite evaluation under these is the real one, not stubbed.

- Spells granted by a chosen optional feature (build order step 6a, final slice part 1) — `src/spells/optionalFeatureSpells.ts`, the THIRD consumer of the `additionalSpells` field. Reuses subclassPreparedSpells.ts's exported helpers (`extractRefs`, `parseSpellRef`, `findSpell`, `isRawSpell`, `isRecord`, `hasConcentration`, `spellIdentityKey`) rather than a third copy of that parsing.
  - Investigation script — scripts/investigate-optional-feature-spells.js (D46), run before the module was written. 19 optional-features.json entries carry `additionalSpells`, but only the 17 with featureType "EI" are reachable: the other 2 are FS:P/FS:R, and per D12 an `FS:*` code resolves against feats.json, so those entries are never offered. Every grant sits under the `"_"` always-granted key — never a class-level key, so no level gate applies and none of the pact-slot-rank machinery is relevant. Keys seen: `innate` (16), `known` (1), `prepared` (1); no `expanded`. 16 of the 17 grant exactly one literal spell (levels 1-4, no cantrips), and every literal ref resolves against spells.json.
  - `ability`: absent on 15 of the 17, the literal `"cha"` on the other 2 (Pact of the Chain, Pact of the Tome) — never `"inherit"`, never a choice object. Charisma IS the Warlock's spellcasting ability and EI is Warlock-only, so even the two that name one name the granting CLASS's own. No grant here is cast with a different ability than the class already uses, so no new spellcasting entry exists to compute and `computeFeatSpellcasting` was left alone; the module deliberately carries no `ability` field.
  - Pact of the Tome was the ONE option that slice left open: its grant is two `choose` filter nodes and no literal ref at all, so it needed a picker with its own storage — the same fixed-vs-choose split every earlier grant slice had (d5a vs d5b-1, d2b vs d6b). Closed by the next entry below.
  - Provenance and merging — `SheetSpellEntry` gained `optionalFeatureOrigins`, `combineSpellEntries` a fourth source, and `provenanceLabel` a `from invocation (Name)` part alongside the existing `player pick` / `always prepared (X)` / `from feat (X)`. Dedupe follows featSpells.ts's precedent exactly: WITHIN one option a repeated spell collapses; ACROSS options it must not, because two options really do grant the same spell — Invisibility, via One with Shadows and Shroud of Shadow (the two refs are written differently, `invisibility|xphb` and a bare `invisibility`, and both resolve to the same identity, which is what lets the merge join them). No option lists the same spell twice today; the per-option dedupe is defensive, same reasoning as featSpells.ts's.
  - These grants are additional and can never count against the spell picker's cantrip/prepared counts — nothing here writes `Character.spellChoices`, and the sheet test asserts the row does not read as a player pick.
  - Sheet — a new `loadOptionalFeatureGrantedSpells` effect in `CharacterSheet.tsx` alongside the featSpells one, feeding the existing `combineSpellEntries` path. Tests in `CharacterSheet.test.tsx`: a Warlock with Mask of Many Faces renders Disguise Self labelled `from invocation (Mask of Many Faces)` (and asserts the loader is actually handed the stored picks, so a mis-wired argument cannot pass), and a Warlock with both shadow invocations shows Invisibility ONCE with both named.

- Pact of the Tome spell picker (build order step 6a) — closes the last open piece of the `additionalSpells` work. Pact of the Tome is the only option in the whole dataset whose grant is a player CHOICE rather than literal spells.
  - Investigation script — scripts/investigate-pact-of-the-tome.js (D46). The grant verbatim: `known._` = `[{choose: "level=0", count: 3}]` and `prepared._` = `[{choose: "level=1|components & miscellaneous=ritual", count: 2}]`, with `ability: "cha"`. The counts come from the DATA (3 and 2) and agree with the 2024 rules text, so nothing is hardcoded the way Ritual Caster's proficiency-bonus count had to be. The cantrip node carries NO class clause at all — any cantrip from any class's list (47 candidates in this app's data); the ritual node likewise has no class clause (12 candidates). `ability` is the Warlock's own Charisma, so as with every other invocation grant no new spellcasting entry appears.
  - Parser — `featSpellChoiceData.ts` generalized IN PLACE for the third time rather than a second parser being written: `parseChooseString`'s filter clause is now optional, yielding a new `{kind: 'any'}` filter that `offeredSpellsForSlot` serves as "every spell at that level, whatever class list it sits on". The script confirmed Pact of the Tome is the ONLY bare `level=` node across feats.json, classes.json and optional-features.json, so the generalization cannot change how any existing feat or subclass parses. featSpellChoiceData.ts's own `parseChooseNode` (which forces a ritual slot's count to null for Ritual Caster) is deliberately NOT reused — Pact of the Tome states `count: 2` outright and that number is honoured.
  - New module — `src/spells/optionalFeatureSpellChoiceData.ts`, sitting to optionalFeatureSpells.ts exactly as subclassSpellChoiceData.ts (d6b) sits to subclassPreparedSpells.ts: `optionalFeatureSpellChoiceShape` (matched on option name AND featureType, same scoping the grant path uses), `offersSpellChoice`, `requiredSpellChoiceCounts`, and loaders through the shared cache (D39).
  - Storage — `CharacterOptionalFeatureChoice` gained `spellChoices?: OptionalFeatureSpellChoice[]`, each `{optionName, cantrips, spells}` keyed by the option's own NAME so a pick stays self-describing if `choices` is reordered. Same `{cantrips, spells}` split as a feat's FilterChoiceSpellsChoice. Schema version bumped to 14 (from 13) — a version-13 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav"). validate.ts gained `describeOptionalFeatureSpellChoicesError`, reusing the existing `describeSpellRefError`.
  - Picker — `OptionalFeatureSpellSubPicker` inside `ClassOptionalFeaturePicker.tsx`, revealed once the option is checked, the same way the feat step reveals its own spell sub-picker. One `SlotPicker` per slot with independently enforced counts (a filled cantrip slot never disables the ritual slot). KNOWN BUG CLASS GUARDED: every callback that writes a stored choice SPREADS the current object rather than rebuilding it from a subset of fields — the d5b-1 bug where a picker's other callback silently dropped already-recorded spell picks. A test picks the spells FIRST and then uses the other control, asserting they survive; another asserts writing one slot never clears the other. Deselecting the option drops that option's own picks (an intentional cascade) while leaving every other option's untouched.
  - Completion gating — `CharacterWizard.tsx` resolves each chosen option's required counts from the data in an effect (`loadOptionalFeatureSpellChoiceShape` through the shared cache, so re-running as the player toggles costs nothing — no second hardcoded count table) and ANDs the result into the existing `classOptionalFeaturesComplete`, so the `classOptionalFeatures` step cannot complete with the picks unfilled. Mirrors `isCompleteFeatAsiChoice`'s gating of a spell-granting feat.
  - Sheet — `optionalFeatureSpells.ts` gained `extractOptionalFeatureChosenSpells`, reading the stored picks DIRECTLY (there is no fixed grant to derive) exactly as featSpells.ts's `extractFilterChoiceSpells` does for a feat; `loadOptionalFeatureGrantedSpells` now returns both halves. A pick whose option is no longer chosen is ignored, so clearing the invocation clears its spells. A `CharacterSheet.test.tsx` test builds a Warlock with Pact of the Tome and picked spells and asserts both render labelled `from invocation (Pact of the Tome)`, not as player picks, and that the stored picks actually reach the loader.
  - Label wording — `provenanceLabel`'s optional-feature part changed from `from option (Name)` to `from invocation (Name)` (Daniel's decision, wording only).

- Senses granted by a chosen optional feature and by a chosen feat (build order step 6a, final piece — closes 6a). Display only at the time this was written; darkvision was reconciled into a calculation change by the follow-up task below.
  - Investigation script — scripts/investigate-senses.js (D46), run before any code was written. 3 optional-features.json entries and 3 feats.json entries carry a `senses` field, every one shaped `[{ <senseType>: <range number> }]` — never a plain string, never more than one sense per grant. Of the 3 optional-features.json entries, only 2 are actually reachable through any picker: Stone Rune (featureType "RN", a Rune Knight rune) and Witch Sight (featureType "EI", a Warlock invocation, granted by the Warlock CLASS's own progression, not a subclass). The third, "Blind Fighting", carries featureType `["FS:F","FS:P","FS:R"]` — every code is `FS:*`, which per D12 resolves against feats.json instead, the same dead-data shape `optionalFeatureSpells.ts` already found for 2 of its 19 `additionalSpells` entries — it can never appear in a stored pick, so no special-case filter was needed for it. All 3 feats.json entries (Blind Fighting, Boon of Truesight, Skulker) are ordinary selectable feats, none hidden.
  - **The darkvision collision the brief asked to check for is real**: Stone Rune grants darkvision, 120 ft. At the time this task was written it was shown as a SEPARATE "Darkvision (Stone Rune)" row in the Senses section, deliberately not merged into `speciesTraits.ts`'s number. **Superseded by the darkvision-reconciliation follow-up task below** — a granted darkvision now merges into `computeDarkvision`'s own value instead. None of the 3 feats.json grants is darkvision (blindsight/truesight only) — already noted in `calculation/featEffects.ts`'s module comment from the earlier feat/ASI slice; this investigation confirms it again against the current data.
  - New module `src/sheet/grantedSenses.ts` — `extractOptionalFeatureGrantedSenses` mirrors `optionalFeatureSpells.ts`'s matching (option NAME + featureType together); `extractFeatGrantedSenses` mirrors `featSpells.ts`'s feat matching (name + source). Both pure (D38), reused `isRecord` from `subclassPreparedSpells.ts` rather than a third copy. `loadGrantedSenses` fetches both files through the shared cache (D39) and returns both sources unmerged.
  - New component `src/sheet/SensesList.tsx` — `combineSenseEntries` mirrors `SpellList.tsx`'s `combineSpellEntries` join shape: a sense TYPE granted by more than one source is shown once, both sources named, range set to the LARGER of the two (senses of the same type don't stack in the 2024 rules) rather than duplicated. No real case shares a type across the 5 reachable grants today; the join exists on the same principle as the spell list's regardless. `senseProvenanceLabel` reuses the exact wording `SpellList.tsx` already established: `from invocation (Name)`, `from feat (Name)`. `<SensesList>` renders nothing at all — no empty heading — when there are no granted senses, same rule the class-optional-features sections already follow.
  - Sheet — wired into `CharacterSheet.tsx` as a new `sheet__senses` section, placed right after `sheet__traits` (speed/size/darkvision). `CharacterSheet.test.tsx`: a Warlock with an invocation chosen renders the granted sense with the invocation named; a Rogue with a granted-sense feat renders it named from the feat; a character with neither renders no Senses section at all.

- Darkvision reconciliation (follow-up to the senses task above). A granted darkvision (Stone Rune) no longer shows as its own Senses row alongside a separate species-darkvision traits row — the two reconcile into ONE value.
  - `speciesTraits.ts`'s `computeDarkvision` gained a third parameter, `grantedDarkvision: GrantedDarkvision[]` (D38 — a plain `{range, origin, name}` shape defined locally, not imported from `sheet/grantedSenses.ts`, to keep `calculation/` from depending on `sheet/`). The result is the LARGEST of the species value and any granted darkvision (senses of the same type don't stack) — a breakdown row per source, but only the winning row carries its real amount; every other row carries 0 plus a `note` (D60's mechanism, generalized past its originally-scoped D55/D58 cases — a documented choice, see docs/REPORT.md) explaining it doesn't exceed the winner, so the breakdown's summed amount still equals the displayed value the way every other D40 breakdown in this app does.
  - D53's parent-fallback and D54's size-choice handling are untouched — only the darkvision path changed.
  - If the species itself has no darkvision field at all (a real, resolved zero — most species), a grant is shown as the whole value with no misleading "species: 0" row, same as before this task for the no-grant case.
  - If the SPECIES darkvision is unresolved (no species chosen, or the species isn't in the supplied data — D43) but a grant exists, the grant is still reported, with a `{source: 'species', amount: 0, note: 'unresolved — ...'}` row so the sheet never presents the grant as though it were a confirmed whole answer.
  - `CharacterSheet.tsx` filters `grantedSenses` into darkvision vs. everything else: darkvision entries feed `computeDarkvision`'s new parameter; every other sense (truesight, blindsight) still goes to `combineSenseEntries`/`SensesList` exactly as before. Darkvision never appears in the Senses section now, regardless of source.
  - Tests: `speciesTraits.test.ts` (+6) covers a larger grant winning, a smaller/equal grant not overriding, a species-with-no-darkvision-field-plus-grant reading as the grant, an unresolved-species-plus-grant reporting the grant with an "unresolved" species note, no-species-with-a-grant, and no-species-no-grant staying unknown (unchanged). `CharacterSheet.test.tsx` (+3): a larger grant updates the traits row and removes the Senses section entirely (single grant, no other sense); a smaller grant leaves the traits row at the species figure; a granted truesight still renders in Senses while the darkvision traits row stays untouched.

- D21 options-node inventory (build order step 6a's earlier investigation — `investigate-class-feature-choices.js` section 5 — named as a count only; this task names all 20). Inventory only: no picker, no storage, no wizard step, no calculation, no sheet change. New script `scripts/investigate-class-feature-options.js`.
  - Confirms the count exactly: 5 class-features.json + 15 subclass-features.json = 20, no feature carries more than one `options` node. Every option item across all 20 is a ref* pointer (6 `refClassFeature`, 74 `refOptionalfeature`, 27 `refSubclassFeature`) — no plain/string option, no `refFeat`, anywhere in this slice of the data.
  - Two uid shapes needed reverse-engineering beyond what `src/featureResolver/resolveRef.ts`'s own doc comment spells out (confirmed empirically against the real data, not assumed): a `refClassFeature`/`refSubclassFeature` uid drops its trailing `|source` segment whenever source equals the default (classSource for a class feature, subclassSource for a subclass feature) — e.g. Primal Order's "Magician|Druid|XPHB|1" (4 parts) targets the record whose real `id` is `cf|magician|druid|xphb|1|xphb` (source repeats classSource); and 2 of the 74 `refOptionalfeature` uids (Bard/Swords' Fighting Style — "Dueling", "Two-Weapon Fighting") carry no source segment at all, unlike every other one — each still resolves unambiguously by name alone against feats.json's category "FS" (D12). Neither shorthand is documented in `resolveRef.ts` itself; flagged here in case that file's own uid-parsing ever needs the same handling.
  - The full 20-line enumeration is reproduced verbatim in docs/REPORT.md, per this task's brief — not repeated here.
  - No judgment made on which of the 20 matter or how any of them should be built; that's Daniel's call once he's seen the list (docs/REPORT.md, "Needs your decision").

- Class-feature choices (D21) — Cleric's Divine Order (1), Druid's Primal Order (1) and Elemental Fury (7): picker, storage, class-step gate, sheet display. What a chosen option GRANTS is deliberately NOT applied (see the deferral note at the end of this entry).
  - Investigation first (D46): `scripts/investigate-options-count.js` established that a `count` on the `options` node is what separates a real choice from a plain container of parts the character receives all of — 9 of the 20 nodes carry one, 11 do not. **This contradicted the task's own scoping assumption**: Barbarian/Storm Herald's three storm features were expected to be choices but carry NO count, even though Storm Aura's own prose says "choose". Raised with Daniel before any code was written; he confirmed building the three class features anyway and leaving Storm Herald to its own task. `scripts/investigate-options-count-2.js` records the prose cross-check. `count` is therefore NOT a sufficient discriminator on its own — see docs/REPORT.md.
  - Second discriminator, `scripts/investigate-class-feature-choice-kind.js`: among CLASS features with a counted node, the ref KIND separates cleanly — 3 are entirely `refClassFeature` (these three, alternate-feature choices), 2 are entirely `refOptionalfeature` (Sorcerer Metamagic, Warlock Eldritch Invocations, which `ClassOptionalFeaturePicker` already owns), 0 mixed. The picker keys off those two structural tests, so no feature is named in code.
  - **`resolveRef.ts` fixed in the shared resolver, not worked around.** A `refClassFeature`/`refSubclassFeature` uid omits its trailing `|source` segment when that source equals the default (classSource / subclassSource): Primal Order points at `"Magician|Druid|XPHB|1"`, whose target's real id is `cf|magician|druid|xphb|1|xphb`. The old code appended the uid verbatim, so Primal Order and Elemental Fury resolved NEITHER of their options — this blocked the task. `withImpliedSource` now restores the segment for both kinds; a uid of any other length is left untouched so a malformed one still fails rather than being reshaped into a wrong match. 3 new resolveRef tests.
  - New module `src/classFeatureChoices/classFeatureChoiceData.ts` — `classFeatureChoicesFrom` (pure, D38) applies both discriminators and resolves each option through `resolveRef` (D51), returning `{featureName, className, classSource, grantedAtLevel, count, options}`; an unresolvable option stays in the list marked `found: false` (D43). `chosenClassFeatureChoicesFrom` joins a stored pick back to its option's TEXT, so the text is re-derived rather than duplicated into storage. `areClassFeatureChoicesComplete` lives here rather than in the picker, matching where `areClassOptionalFeaturesComplete` sits.
  - New component `src/classFeatureChoices/ClassFeatureChoicePicker.tsx` — a radio group, not checkboxes: each of these has count 1 and replaces the feature with exactly one alternative, unlike the progression pickers that accumulate with level. Every option renders its own resolved text (D13). Wired into the CLASS step (D13; none of the three depends on spells, so D64's exception does not apply), cleared when class or level changes, not on subclass.
  - Class step gate: `isStepComplete('class', ...)` gained a `classFeatureChoicesComplete` flag, computed in `CharacterWizard.tsx` from the loaded choices via the same pure function the picker renders from, so gate and UI cannot disagree — the same pattern `classOptionalFeaturesComplete` already uses. The step cannot advance, and the review step cannot save, while a granted choice is unmade.
  - Storage: `Character.classFeatureChoices` (`CharacterClassFeatureChoice[]` — className/classSource per D11, featureName, `grantedAtLevel` per D22, optionName). `describeClassFeatureChoicesError` added to validate.ts; `create` gained a trailing parameter. Schema version bumped to 15 (from 14) — a version-14 save is rejected, not migrated (docs/QUESTIONS.md, "Migrace uložených postav").
  - Sheet: a `sheet__class-feature-choices` section, one collapsed `<details>` per pick showing the chosen option's resolved text and naming the feature and its level — the same shape the feat list and class-optional-features sections use. Renders nothing at all when no choice was made.
  - The order-of-use bug this project has shipped TWICE (d5b-1's sheet fix, then Pact of the Tome) is guarded explicitly: the write callback spreads the entry it replaces, and a test picks for two features in both orders and asserts neither erases the other. The test was verified to FAIL against a deliberately reintroduced version of the bug before being kept.
  - **Deferred on purpose, nothing applied:** Thaumaturge's and Magician's extra cantrip, and Protector's/Warden's armour and weapon proficiencies, are all recorded and displayed but feed no calculation. Armour/weapon proficiencies have nowhere to land until build order step 7 (Inventory and equipment) — which is why this task ran first, so step 7 finds the choice already stored. The extra cantrip is its own small follow-up.

## Next step

Build order step 6 is done except for standing deferrals: a decision on the
`expanded`-key deferrals (docs/REPORT.md) and on Boon of Siberys (still
hidden/deferred), before step 7 — Inventory and equipment. Every picker
slice (d2, d2b, d5a, d5b-1/2/3, d6a, d6b) is closed, and feat-spell
attack/DC now displays alongside class spellcasting.

Step 6a is closed (data layer, picker, storage, sheet, its own wizard step):
the whole `additionalSpells` half is done (16 fixed grants derive, Pact of
the Tome's choice has its own picker), and the `senses` half is done too (2
reachable optional-feature grants, 3 feat grants, displayed with joined
provenance). Next up is step 7 — Inventory and equipment — once the standing
step-6 deferrals above are decided.

The D21 class-feature choices are partly built: the three CLASS ones (Divine
Order, Primal Order, Elemental Fury) are stored and displayed. Three follow-ups
stand open, each its own task: applying what those options grant (the extra
cantrip now; armour/weapon proficiencies at step 7), Barbarian/Storm Herald's
three storm features (one choice at level 3 that two later features reference —
and the `count` test does not detect it; recorded in docs/QUESTIONS.md,
"Storm Herald — volba prostředí je jen v próze"), and the remaining
subclass-level features from the 20-feature inventory.

Note: the class-specific selections (including feat/ASI choices) are still not recorded with the
level they were taken at as a SEPARATE provenance field — for feat/ASI the
level is inherent in the array's own shape (one entry per level), but D22
proper (recording it for every OTHER choice type too) is still deferred —
see docs/QUESTIONS.md. Attacks and AC (build order steps 7/8) will need
their own pass over the same 15 prose feats plus the weapon/armor/tool
proficiency structured fields this task left untouched (docs/REPORT.md has
the feat list). The 5 skill-choice feats with no stored pick (docs/REPORT.md)
are a standing gap for whenever the wizard adds that field.

## Temporary scaffolding

- src/CharacterManager.tsx — list/rename/delete/export/import UI; replaced by the sheet (step 5).
- src/CharacterInspector.tsx — read-only dump of a stored character; replaced by the sheet (step 5) and now unwired (no import or button references it anywhere), but still not deleted — the same settings deny rule has blocked the delete command across two sessions now (docs/REPORT.md). Delete by hand.
- src/MarkupDemo.tsx — renders sample data/ entries through the markup renderer; replaced by creation (step 3) and the sheet (step 5).
